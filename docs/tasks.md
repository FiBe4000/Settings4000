# Settings4000 — Implementation Tasks

Atomic, non-overlapping breakdown of `docs/architecture.md`. Requirement references (R…) point to `docs/requirements.md`.
Complexity: 1 = simple/repetitive … 5 = highly complex.

## 1. Project foundation

- [x] **1.1 Cargo project scaffold** — Complexity: 1
  Init binary crate (2024 edition) with `gtk4`, `relm4`, `tracing`, `tracing-subscriber`, `tracing-journald`, `serde_json` (preserve_order), `tempfile` (dev), `nix` deps; module tree `ui/ core/ parsers/ system/` as in architecture §2. *Accept:* `cargo build` and `cargo test` pass on empty modules; a module-boundary check (grep-based test for `gtk` imports, or workspace crate split) fails if `core/` or `parsers/` import `gtk`.
  *Done:* scaffolded in `Cargo.toml` + `src/` with a `tests/` guard:
  - 2024-edition binary crate `settings4000` with the required dependencies (`gtk4`/`relm4`, the `tracing` stack, `serde_json` with `preserve_order`, `nix`; `tempfile` as a dev-dependency) and the `ui/ core/ parsers/ system/` module tree from architecture §2.
  - `tests/module_boundaries.rs` is a grep-based guard that fails if any file under `src/core` or `src/parsers` imports `gtk`/`gtk4`/`relm4`, enforcing the GTK-free layering rule (R6.2); confirmed to catch real violations.

- [x] **1.2 CLI & logging init** — Complexity: 2
  `--log-level` flag; `EnvFilter` from `SETTINGS4000_LOG`/`RUST_LOG` (flag wins); journald layer with stderr fmt fallback when journald is unavailable (R7.1–R7.2). *Accept:* messages visible in `journalctl --user -t settings4000`; fallback exercised by unit test with journald socket absent.
  *Done:* implemented in `src/system/logging.rs`, wired from `main.rs`:
  - A `--log-level` flag plus an `EnvFilter` resolved from `SETTINGS4000_LOG`/`RUST_LOG` with the flag taking precedence; a blank or malformed directive degrades to `info`. `--log-level debug` raises only the `settings4000` target so dependency chatter doesn't bury the app's own logs.
  - Tracing init attaches a journald layer (syslog identifier `settings4000`) and falls back to a stderr `fmt` layer when journald is unavailable; the backend selection is injectable and unit-tested (fallback exercised without a live journald).

- [x] **1.3 GtkApplication bootstrap + single instance** — Complexity: 2
  Fixed app ID, `Application::register`; second launch activates the existing window and exits (R8.4). Empty `ApplicationWindow` shown. *Accept:* relaunching focuses the running window; process exits 0.
  *Done:* implemented in `src/ui/app.rs`, wired from `main.rs`:
  - Fixed ID `org.settings4000.Settings4000` with default (unique) flags; an explicit `register()` selects the primary/remote role, then `run_with_args` drives it — the primary shows an empty window on `activate`, while a relaunch (`is_remote()`) forwards `activate` to present the running window and exits 0.
  - `main` parses the CLI with clap and forwards only `argv[0]` to GTK, so `--log-level` is never re-parsed by GApplication. The app-ID validity is unit-tested; relaunch-focus and exit-0 were verified live on the target session.

- [x] **1.4 CI / pre-commit gate** — Complexity: 1
  Wire `cargo fmt --check`, `cargo clippy -D warnings`, `cargo test` into CI and/or pre-commit (R6.1). *Accept:* pipeline fails on any of the three.
  *Done:* the gate runs as GitHub Actions and, optionally, as a local hook:
  - `.github/workflows/ci.yml` runs the three checks as separate steps on push to `main` and on every pull request; a job fails as soon as any step exits non-zero, so the run fails on any one gate. It installs `libgtk-4-dev` (the `gtk4` crate needs system GTK to compile) and uses the version-pinned `actions/checkout@v4` and `Swatinem/rust-cache@v2` actions, plus `dtolnay/rust-toolchain@stable` which tracks the current stable Rust channel (rustfmt + clippy).
  - `.githooks/pre-commit` mirrors the same three checks for local use; it is opt-in and version-controlled — enable it per clone with `git config core.hooksPath .githooks`.
  - Decision: clippy runs with `--all-targets` so test code is linted too; each gate was verified to fail on an injected violation and pass on the clean tree.

## 2. System boundary

- [x] **2.1 `CommandRunner` trait + real impl** — Complexity: 2
  Shell-free `Command` spawning (arg vectors), 5 s timeout, captured exit status/stderr; every invocation logged (cmd + exit, R7.3). Mock recorder impl for tests. *Accept:* unit tests via mock; real impl runs `true`/`false` correctly with timeout kill verified.
  *Done:* implemented in `src/system/command.rs`:
  - A `CommandRunner` trait over a shell-free `Command` (program + arg-vector builder — no `sh`, no string interpolation) and a real `SystemCommandRunner`. `run` returns `Ok(CommandOutput)` (captured exit code + full stdout/stderr; `success()` == exit 0) for a completed process, and a `CommandError` distinguishing spawn failure, timeout, and wait failure. Both pipes are drained on per-stream reader threads concurrently with the timed wait, so a large-output command (e.g. `pw-dump` JSON, well over the 64 KiB pipe buffer) cannot block on write and deadlock. Each invocation and its exit status is logged at `info` (R7.3) with a truncated stderr excerpt only at `debug`.
  - The 5 s deadline is the named `DEFAULT_TIMEOUT` constant (architecture §6); a child that overruns it is killed and surfaces `CommandError::Timeout`. The wait-with-deadline uses the new `wait-timeout` = "0.2.1" dependency (race-free OS wait, no busy-poll). Tests inject a shorter timeout to verify the kill quickly while production stays at 5 s.
  - A `#[cfg(test)]` `MockCommandRunner` records the exact program+args sequence and returns queued canned outcomes (defaulting to success), so callers can be tested without spawning processes (R6.1).

- [x] **2.2 Atomic file writer** — Complexity: 3
  Files are addressed by their XDG runtime path (`~/.config/…`), never a hardcoded `~/.dotfiles` path (R8.5). `fs::canonicalize` the target (a symlink into a dotfiles repo resolves to the repo file; a plain file resolves to itself) → `NamedTempFile` in the resolved target's dir → fsync → rename over the target (R5.4). Symlink preserved when present; plain files rewritten in place. Pre-write snapshot capture for rollback; write logged with resolved path (byte-level — changed-key logging lives in the apply orchestrator, R7.3). *Accept:* tests prove symlink target rewritten + link preserved, a plain (non-symlink) file rewritten in place, no partial file on injected failure, snapshot restore works.
  *Done:* implemented in `src/system/writer.rs`:
  - `write_atomic(path, bytes)` canonicalizes the live XDG path (R8.5), snapshots the current bytes, then stages the new content in a `tempfile::NamedTempFile` in the resolved target's directory, fsyncs it, and atomically renames it over the resolved target (plus a best-effort parent-dir fsync). Renaming over the *resolved* path rewrites the real file and leaves any symlink into a dotfiles repo intact; a plain file is rewritten in place (R5.4). Original file permissions are preserved so the swap doesn't tighten a 0644 config to the temp file's 0600 (R8.3).
  - The content step is a closure seam (`atomic_write_with`), so a test can inject a failure before the rename and prove the original is untouched with no stray temp file (the `NamedTempFile` deletes on drop). `write_atomic` returns a `FileSnapshot` capturing the original bytes *and* permission mode, whose `restore()` puts both back through the same atomic path — the per-file rollback the Apply pipeline (task 4.5) will use. Each write logs its resolved path + byte count at `info`, never contents (R7.3).
  - `tempfile` moved from a dev-dependency to a regular dependency because `NamedTempFile` is now used in production code (still available to tests).

- [x] **2.3 File freshness tracking** — Complexity: 2
  Per-file content-hash + mtime record on read; `check_conflicts()` re-reads and reports changed files (R5.6). *Accept:* external modification between read and check is detected; unchanged files aren't flagged.
  *Done:* implemented in `src/core/freshness.rs` (domain logic, GTK-free — architecture §3 "Write safety" and §6 step 2):
  - `FreshnessTracker::record(path)` reads a file and stores a per-file baseline of a content hash (`std`'s SipHash `DefaultHasher`, so no new dependency) plus its mtime; `record_bytes(path, bytes)` records the same baseline from bytes the caller already read (the store's read-time entry point, task 4.2) — avoiding a second read and closing the TOCTOU gap where an external edit between the store's read and a tracker re-read would baseline the edited bytes; `check_conflicts()` re-reads every tracked file and returns a path-sorted `Vec<Conflict>` (each a tracked path + reason) of those that changed since it was recorded (R5.6). It lives in `core/` — not `system/` — because it is store/apply-pipeline domain logic (tasks 4.2/4.5) and reading files to compare them needs no side-effect abstraction.
  - Conflict rule: the content hash is authoritative — a file conflicts iff its bytes no longer hash to the recorded value (`ContentChanged`) or it can no longer be re-read (`Unreadable`, e.g. deleted or permission-revoked, carrying the OS error); the recorded mtime is diagnostic only and never gates the decision, so a content edit made under an unchanged mtime is still caught and a pure mtime touch (or a rewrite to identical bytes) is not a false positive.

## 3. Parsers (each: lossless representation, targeted value edits, round-trip tests — R5.3 item 1, R6.1)

- [x] **3.1 Palette kv parser (`colors/<scheme>`)** — Complexity: 2
  Line-oriented `key=barehex`; in-place value edit; validation against the fixed 17-key schema. Write path exists only for R6.1 round-trip coverage / future per-key editing — no v1 UI edits `colors/<scheme>` (R3.2 switching runs `generate-colors` only; per-key palette editing is out of scope for v1). *Accept:* round-trip identity; edit changes exactly one value span; malformed lines surfaced as parse warnings, not panics.
  *Done:* implemented in `src/parsers/palette.rs` (GTK-free parser, architecture §3):
  - Lossless representation: the file is kept as its lines in order, each retaining its exact raw bytes (terminator included) so `PaletteFile::emit()` concatenates them back byte-for-byte; a `key=value` entry additionally records the byte span of its value within that line. `parse()` never fails and preserves every line verbatim.
  - Edit API: `set_value(key, value)` splices only the recorded value span of the first matching entry, leaving indentation, spacing around `=`, trailing whitespace, comments, and all other lines byte-identical; it rejects a non-bare-hex value (`InvalidValue`, R8.3) or an absent/comment-only key (`UnknownKey`) before touching any byte. `value(key)` reads a key's current value.
  - Schema validation: `validate()` checks entry keys against the fixed 17-key schema (`PALETTE_KEYS`), reporting out-of-schema, missing, and duplicate keys; valid only when all three are empty (mirrors `generate-colors`' key-presence check, analysis §6.4). Value-format is a separate concern from key-presence.
  - Warning handling: malformed lines (no `key=value` shape) and non-hex values are returned as `ParseWarning`s (line number + kind) and logged at `warn` — never panics, never data loss; comments/commented-out entries are never matched by edits or counted as entries.

- [x] **3.2 hyprlang parser/writer** — Complexity: 5
  Tokenize comments, `key = value`, nested `section { }`, `source=`. Edits address keys by section path (`input.touchpad.natural_scroll`); repeatable top-level keys (`env=`, `exec-once=`) addressed by key + first comma-field (e.g. `env:XCURSOR_THEME`) with only the value portion edited; commented-out lines never matched; new keys appended at section end. Covers `config/hypr/input.conf` (the extracted `input { }` block), `hyprland.conf` (cursor `env=`), `hypridle.conf`, `hyprlock.conf`, `hyprpaper.conf`. *Accept:* round-trip identity on live-dotfile fixtures; targeted edits leave all other bytes untouched; nested + duplicate-section fixtures pass; the `input.*` fixture edits `input.conf` (e.g. `input.touchpad.natural_scroll`), not `hyprland.conf`; repeatable-key fixture (multiple `env=` lines, edit `env:XCURSOR_THEME` only) passes.
  *Done:* implemented in `src/parsers/hyprlang.rs` (GTK-free parser, architecture §3):
  - Token model: the file is kept as its lines in order, each retaining its exact raw bytes (terminator included) so `HyprlangFile::emit()` reproduces the input byte-for-byte; each line is classified as blank, comment, `section {` open, `}` close, `key = value` entry (recording the value's byte span, which stops before any trailing inline `# comment`), or malformed. An entry's key is the whitespace-free token before the first `=`, so hyprland's dotted keys (`col.active_border`) and `$variable` declarations classify correctly. Malformed lines and a `}`/header imbalance are surfaced as `ParseWarning`s and logged at `warn`, never panicking or losing data.
  - Section-path addressing (`KeyPath`): a value is named by a section path plus key; duplicate sections are told apart by 0-based occurrence (`SectionStep::nth`, e.g. hypridle's `listener[2]`), resolved by walking the open-section stack with per-parent occurrence counts so nesting and duplicates compose, and `section_count` enumerates repeated blocks (task 6.8's listeners). `input.*` paths resolve in `input.conf`, not `hyprland.conf`.
  - Repeatable-key addressing (`set_repeatable_field_value`): a top-level key (`env`, `source`, `exec-once`) is selected by its first comma-field and only the value portion after that comma is rewritten (e.g. `env:XCURSOR_THEME` → the cursor theme), leaving the field and siblings byte-identical; commented-out lines never match.
  - Append: `set_value` on a missing key inserts a new assignment just before its target section's closing `}` (or at end-of-file for a top-level key), copying an existing sibling's indentation and `=`-separator style, or falling back to the header indent + four spaces and ` = `. Values containing a newline or `#` are rejected before any byte changes (R8.3). Covered by round-trip identity on `input.conf`/`hypridle.conf`/`hyprpaper.conf`/`hyprland.conf`/`hyprlock.conf` fixtures plus targeted-edit, nested-and-duplicate-section, repeatable-key, commented-out, append, and malformed-input tests.

- [x] **3.3 monitors.conf record parser** — Complexity: 3
  Parse `monitor=` records (name/desc, mode, position, scale, enable — the latter handled as `monitor=NAME,disable` records) preserving surrounding lines and later-rule-wins order; edit matching record or append after last `monitor=` line. *Accept:* round-trip identity; editing the eDP rule doesn't disturb the catchall or comments.
  *Done:* implemented in `src/parsers/monitors.rs` (GTK-free parser, architecture §3):
  - Lossless line model like the sibling parsers: each `monitor=` record keeps its raw bytes plus the trimmed byte span of every comma-separated field (index 0 = name, 1 = mode, 2 = position, 3 = scale, 4.. = extras) and a `disabled` flag for the `NAME,disable` form; `emit()` reproduces the input byte-for-byte and records are never reordered, so Hyprland's later-rule-wins order is preserved.
  - `set_field` rewrites exactly one field span (mode/position/scale) in place, and `set_state` enables/disables a record or **appends** a new one after the last `monitor=` line (copying the file's `monitor=` separator style); both reject a comma/newline/`#` in a written value so the record stays parseable by the awk in `scripts/hypr-display-profile.sh` (name field 1, mode field 2, scale field 4) — the app never touches that script.
  - Built as a focused standalone parser rather than on the hyprlang line model: hyprlang addresses a repeatable key by its first comma-field and edits the rest as one blob, whereas this needs positional field addressing (match by name, edit arbitrary fields) specific to `monitors.conf`. Malformed/unexpected `monitor=` lines are preserved and surfaced as warnings (no `unwrap`/panic).

- [x] **3.4 swaync JSON adapter** — Complexity: 1
  `serde_json` preserve_order, parse–modify–reserialize (2-space pretty). *Accept:* key order stable across a no-op edit; position/timeout edits round-trip.
  *Done:* implemented in `src/parsers/swaync.rs` (GTK-free adapter, architecture §3):
  - `SwayncConfigFile::parse` decodes `config.json` into a `serde_json` object map (the crate's `preserve_order` feature backs objects with an index map, so key order is retained); `emit()` re-serializes with the 2-space pretty printer plus a trailing newline — swaync's exact on-disk shape, verified by a byte-identity round-trip test. Malformed JSON returns `ParseError::InvalidJson` and a non-object root `ParseError::NotAnObject`; neither panics.
  - Get/set API is by key with typed access for the Notifications page (task 6.7): `string`/`set_string` (position enums `positionX`/`positionY`), `integer`/`set_integer` (`timeout`/`timeout-low`/`timeout-critical`), and `boolean`/`set_boolean` (top-level flags such as `keyboard-shortcuts`/`fit-to-screen`). Getters return `None` for an absent or wrongly-typed key; setters update in place (preserving key order) or append a new key. Domain validation (enum/range) is intentionally left to task 4.1/6.7. Note: swaync's do-not-disturb is runtime daemon state (via `swaync-client`), not a config boolean, so task 6.7 must confirm the DND mechanism rather than write a `dnd` key.
  - Formatting caveat: JSON is normalized, not byte-preserved — a file already in 2-space-pretty-plus-newline form round-trips exactly, while other formatting (different indent, no trailing newline) is rewritten to the canonical form. This is the only unavoidable difference and is expected for a comment-free JSON file.

- [x] **3.5 INI editor (GTK settings.ini)** — Complexity: 2
  Line-preserving kv-in-section edits. Both `settings.ini` files are now tracked in the dotfiles and symlinked (analysis §6.5), so editing an existing `[Settings]` section is the common path; creating file + `[Settings]` section when absent remains a fallback. *Accept:* comments/order preserved; both the edit-existing and file-creation paths tested.
  *Done:* implemented in `src/parsers/ini.rs` (GTK-free editor, architecture §3):
  - Lossless line model like the sibling parsers: each line keeps its raw bytes (terminator included) and a classification (blank / `#` comment / `[section]` header / `key=value` entry with the value's byte span / malformed), so `IniFile::emit()` reproduces the input byte-for-byte and a malformed line (e.g. an unclosed `[`) is preserved and surfaced as a `ParseWarning`, never a panic.
  - `set_value(section, key, value)` unifies all three paths, reporting which happened via `SetOutcome`: **edit-existing** (the common case on the dotfiles machine) splices only the value span inside an existing `[Settings]`; **append** inserts an absent key at the section's end (after its last non-blank line, copying a sibling entry's indent/separator style); **create-from-scratch** builds `[Settings]` + the key at EOF (from `IniFile::empty()` or a file lacking the section). Values with a newline/carriage return, and unusable keys/section names, are rejected before any byte changes (R8.3).
  - Per-file by design: the Theme page (task 6.4) drives both `gtk-3.0`/`gtk-4.0` files by applying identical `set_value` calls to each `IniFile` (covered by a test). Caveat: this produces the *bytes* of a created file only — physically creating an absent `settings.ini` on disk is a writer/Apply concern (the atomic writer only rewrites existing targets today), deferred to tasks 4.5/6.4.

- [x] **3.6 env-file editor (`uwsm/env`)** — Complexity: 2
  Targeted `export KEY=value` edits for `XCURSOR_THEME`/`XCURSOR_SIZE`; detects (but never edits) commented lines — feeds the `GTK_THEME` override warning (R3.3). *Accept:* round-trip identity; commented `GTK_THEME` reported, uncommented lines editable.
  *Done:* implemented in `src/parsers/env.rs` (GTK-free editor, architecture §3):
  - Lossless line model like the sibling parsers: each line keeps its raw bytes (terminator included) and a classification (blank / `#` comment / `KEY=value` assignment with the value's byte span / malformed), so `EnvFile::emit()` reproduces the input byte-for-byte and only a genuinely uninterpretable line (no `=`) is surfaced as a `ParseWarning`, never a panic. Because uwsm sources the file with allexport, both `export KEY=value` and a bare `KEY=value` are recognized as assignments.
  - `set_value(key, value)` rewrites only the value span of the **last** (shell-effective) assignment for the key (either form), or appends `export KEY=value` at EOF when the key is absent — value-only so the Theme page (task 6.4) can drive this copy and the other `XCURSOR_*` copies from one value. Quotes are treated as part of the value span (never parsed/synthesized); a written value with a newline/carriage return is rejected (R8.3).
  - `gtk_theme_override()` reads (never edits) `GTK_THEME` for R3.3, distinguishing an active `GTK_THEME=…` (export or bare → banner + disabled drop-down) from a commented-out one (`#export …`, `# export …`, or `# GTK_THEME=…`, present but inactive) and absent; a commented line is never matched by a value-set call.

- [x] **3.7 Generated-file readers** — Complexity: 1
  Read-only: active-scheme detection from the `# Generated from colors/<scheme>` header of the deployed `~/.config/hypr/colors.conf` (R3.2); optional per-scheme swatch parse (reads the discovered repo's `colors/<scheme>` files, R8.5) for the drop-down. The repo also carries a `state/active-scheme` marker (analysis §6.4) as an optional future detection shortcut; v1 reads the header. *Accept:* scheme name extracted from fixture; missing/odd header degrades to "unknown" without error.
  *Done:* implemented in `src/parsers/generated.rs` (GTK-free, read-only — the only parser module with no write/edit path, since the generated files must never be written by the app):
  - Active-scheme detection reads the `# Generated from colors/<scheme>` header of the deployed generated file (v1: `~/.config/hypr/colors.conf`) and returns a typed `ActiveScheme::Named(name)` or `ActiveScheme::Unknown` (R3.2). Recognition is tolerant (leading whitespace, optional space after `#`, collapsed internal whitespace, ASCII-case-insensitive prose and `colors/` prefix) but does not over-match; a missing header, an odd/malformed header, an empty file, or an unreadable file all degrade to `Unknown` — never an `Err`/panic. The `state/active-scheme` marker is noted as an optional future shortcut but v1 reads the header.
  - Per-scheme swatch parse (`parse_scheme_swatch`/`read_scheme_swatch`, R8.5) reuses the task-3.1 palette parser to read the 17 schema colors (in canonical order) plus the schema-validity report into a read-only `SchemeSwatch` for the theme drop-down (task 6.3); it degrades gracefully on a malformed palette (fewer colors + a validation report, no panic). Callers pass file contents or a live path (repo-root discovery is task 4.3).

## 4. Core domain (GTK-free — R6.2)

- [x] **4.1 Typed settings model + validators** — Complexity: 3
  `SettingId`, typed values (enum/bool/float/string), per-setting validation: hex colors, `WxH@Hz` mode strings, timeout/sensitivity/scale ranges, file paths (exists + readable + image extension for wallpaper/lock background) (R8.3). *Accept:* unit tests per validator, valid + invalid cases.
  *Done:* implemented in `src/core/model.rs` (GTK-free domain logic — R6.2):
  - `SettingId` is a fieldless enum — the central registry of every editable setting, used as the store's key (task 4.2) and validated by the Apply pipeline (task 4.5). Each variant declares its `kind()` (a `ValueKind`) and `category()` (its sidebar page, for the store's per-page dirty rollup); a page adds a setting by adding a variant and extending those mappings plus the `validate` match (task 4.2 later added a `backing()` mapping — file-backed vs runtime-only — alongside `kind()`/`category()`/`validate()`). `Value` is a typed union — `Enum`/`Bool`/`Float`/`Integer`/`String` with typed accessors — where `Integer` is kept distinct from `Float` so whole-number settings (timeouts) never gain a fractional part or emit `300.0` into a config expecting `300`.
  - `SettingId::validate(&Value)` first requires the value's kind to match (`WrongKind`), then dispatches to the value-format validator; the returned `ValidationError` renders a UI-ready message via `Display` (R8.3). Validators: bare six-hex-digit color (reuses the task-3.1 `is_bare_hex`); monitor mode accepting a bare `WxH`, a full `WxH@Hz`, or the `preferred`/`highres`/`highrr` tokens (dimensions 1–16384 px, and when present a 1–1000 Hz refresh); numeric ranges `SENSITIVITY_RANGE` (-1.0..=1.0), `SCALE_RANGE` (0.5..=3.0), `TIMEOUT_SECONDS_RANGE` (1..=86400 s); and an image-path check (exists + regular file + png/jpg/jpeg/webp extension + readable, following symlinks, no panic on an IO error).
  - Unit-tested per validator with valid and invalid cases (hex length/chars/`#`-prefix; malformed and out-of-range modes; range boundaries and just-outside; image path missing/directory/non-image/unreadable via a `tempfile` fixture), plus dispatch, wrong-kind, and `Display`-message coverage; the module-boundary guard confirms `core/` stays GTK-free.

- [x] **4.2 SettingsStore (staging state machine)** — Complexity: 3
  `original`/`staged` per setting; dirty computation, per-page dirty rollup, `stage()`, `reset()`, conflict-triggered reload of originals (R5.1, R5.6). Runtime-only settings bypass staging (R5.2). *Accept:* headless tests cover stage→dirty→reset→clean, conflict reload, and bypass paths.
  *Done:* implemented in `src/core/store.rs` (GTK-free domain logic — R6.2), building on `model` (task 4.1) and `freshness` (task 2.3):
  - Original/staged model: `SettingsStore` keeps, per file-backed `SettingId`, an `original` `Value` (as read from disk) and an optional `staged` `Value` (the pending edit); dirty = a staged value that differs from the original (a staged value *equal* to the original is not dirty, so re-selecting the current value never lights up Apply). `stage(id, value)` records the edit, `value(id)` returns the effective (staged-or-original) value, and `original(id)` the baseline.
  - Dirty + rollup: `is_dirty()` drives the global Apply state, `is_category_dirty(Category)` is the per-page modified-marker rollup, and `dirty_ids()` lists the changed settings in `SettingId` order for the Apply pipeline (task 4.5). `reset()` discards every staged edit, returning the store to clean.
  - Conflict-reload (R5.6): the store owns a `FreshnessTracker` and baselines each file via `record_bytes(path, &bytes)` — the exact bytes the loader parsed the original from, never a re-read (avoids the double-read + the read→baseline TOCTOU window per the 2.3 review). `refresh()` re-reads the tracked files, and for each that changed externally reloads its originals from disk through a per-file reader closure (keeping the store parser-agnostic) and re-baselines; pending staged edits are preserved (no silent clobber), and the `RefreshReport` tells the UI which files reloaded vs. failed (deleted/unreadable).
  - Runtime-only bypass (R5.2): a new `Backing` marker on `SettingId` (`SettingId::backing()`) classifies each setting; `stage()` returns `StageOutcome::RuntimeBypass` for a runtime-only setting (the laptop-display toggle, added as the first such variant) without storing anything, so it is never staged and never dirty. The store's map holds only file-backed settings, so dirty/rollup/reset exclude runtime ones automatically.
  - Dirty-robustness (the 4.1 review, `Value` is `PartialEq` not `Eq`): `stage()` validates every value via `SettingId::validate` first, and the range validators reject a non-finite float, so a `NaN` can never enter `staged` and cause a permanently-dirty setting; `reset()` is independently safe because it clears `staged` to `None` (dirty is false without a staged value).
  - Tested headless: stage→dirty→reset→clean, per-page rollup, external-change conflict reload (tempfile edited between load and refresh) with re-baselining and staged-edit preservation, the runtime-only bypass, and the NaN/out-of-range/unloaded validate-on-stage cases.

- [x] **4.3 Capabilities detection** — Complexity: 3
  Manual `$PATH` scan (injected PATH string) — including `hyprctl` (needed by nearly every reload), `gsettings`, `pactl`/`wpctl`, and the daemon binaries (`kitty`/`eww`/`swaync`/`hyprpaper`/`hypridle`/`hyprlock`) — daemon liveness (Hyprland IPC socket path, procfs pidof-equivalent), `nmcli` presence, settings-portal availability (`xdg-desktop-portal-gtk`/dconf — gates the R2.2 live-restyle claim), the dotfiles palette source (repo root canonicalized from a deployed config symlink, then `colors/` + `scripts/generate-colors` present — gates the palette switcher, R3.2/R8.5), config readable/parseable checks at XDG paths (R4.1, R4.4). Produces plain `Capabilities` struct; errors degrade to absent; run once at startup + manual refresh (R4.3). *Accept:* fake-PATH tests (R6.1); non-symlinked config (no repo behind it) hides the palette switcher; hidden items logged at `info`, unreadable configs at `warn`.
  *Done:* implemented in `src/core/detect.rs` (GTK-free domain logic — R6.2), reading env/filesystem/procfs/sockets directly (never via `CommandRunner`/a subprocess, since reading is not a process side effect and a `which` per binary would cost cold-start budget):
  - Injectable probe design (R6.1): a `DetectionInputs` bundle carries the raw, faked-in-tests inputs — a `$PATH` **string** for the binary scan, a list of running process names for daemon/portal liveness, and **paths** for the Hyprland socket, the palette anchor, and the backing configs (checked against the real filesystem, so tests build a temp-dir fixture and get faithful behavior). `DetectionInputs::from_system` gathers the real inputs (env `$PATH`, a `/proc/*/cmdline` scan, the `$XDG_RUNTIME_DIR` socket path, the XDG config anchor); `Capabilities::detect` is the pure, re-runnable routine over any inputs, so a manual refresh (R4.3) just re-gathers and re-calls it.
  - Capability set: a `Binary` scan (manual colon-split `$PATH` walk with a Unix exec-bit check) for `hyprctl`/`gsettings`/`pactl`/`wpctl`/`kitty`/`eww`/`swaync`/`hyprpaper`/`hypridle`/`hyprlock`/`nmcli` plus `dconf`; a `Daemon` procfs liveness scan (`kitty`/`eww`/`swaync`/`hyprpaper`/`hypridle`) matching processes by **untruncated argv[0] basename** (from `/proc/<pid>/cmdline`, exact match); Hyprland liveness via the IPC socket path's existence; a settings-portal signal detecting a Settings-implementing backend (`xdg-desktop-portal-gtk`/`-gnome`/`-kde`) by its **full name** — not the shared 15-byte `/proc/comm` prefix (`xdg-desktop-por`) the whole `xdg-desktop-portal-*` family collapses to, which would falsely claim R2.2 live restyle whenever the base/`-hyprland` portal runs — or the `dconf` backend; and per-config readability (R4.4). Query accessors (`has_binary`, `audio_available`, `is_daemon_live`, `hyprland_reloadable`, `settings_portal_available`, `palette_source`, `config_readable`) are the surface tasks 4.4/4.5/5.x/6.x consume.
  - Repo-root discovery (R3.2/R8.5): `symlink_metadata` first requires the palette anchor (`~/.config/hypr/colors.conf`) to be a **symlink** — a plain file means no dotfiles repo behind it, so the switcher is hidden (an explicit accept criterion) — then canonicalizes it, verifies and strips the known `config/hypr/colors.conf` tail to derive the repo root (a symlink resolving elsewhere is rejected), and reports a `PaletteSource` (repo root + `colors/` + `scripts/generate-colors`) only when both sources exist. The resolved repo root is exposed for tasks 3.7/6.3/4.5.
  - Degrade-to-absent (R4.3/R4.4): no probe failure aborts detection — a missing `$PATH`, an unreadable `/proc`, a dangling symlink, or an unreadable config each yield the "absent" capability, so `detect` returns a plain value and never a `Result`. Logging (R7.3): every hidden-palette-source reason and absent page-gating binaries/portal at `info`, missing/unreadable configs at `warn`, a one-line summary at `info`, never file contents (the absent wpctl/pactl message is softened, since either client satisfies audio).
  - Tested headless with injected inputs: a fake temp-dir `$PATH` flips a binary present↔absent (R6.1); a config symlinked into a fake repo with `colors/`+`generate-colors` yields the palette source while a non-symlinked (plain) config, an incomplete repo, and a symlink resolving outside the expected layout each hide it; a supplied process list and socket path drive daemon/portal/Hyprland liveness present and absent without a live desktop, including that a base/`-hyprland` portal without `-gtk`/dconf does **not** claim live restyle; a missing/permission-revoked config degrades to absent; and a scoped tracing subscriber verifies the info/warn log levels.

- [x] **4.4 Reload command table** — Complexity: 2
  Map backing-file → reload action: `hyprctl reload`, `eww reload`, `swaync-client -rs`, SIGUSR1 to kitty PIDs via `nix::kill`, `hyprctl hyprpaper …`, hypridle restart (`systemctl --user try-restart` else kill+respawn), `gsettings set …` + `hyprctl setcursor` (architecture §6 table). This set mirrors `scripts/apply-theme` (the canonical CLI wrapper, analysis §6.1) and must stay in sync; kitty uses SIGUSR1 in v1 though remote control is now enabled for a later flicker-free `kitten @ set-colors` (analysis §6.1). *Accept:* unit tests: each changed file maps to exactly the expected actions, gated on capability.
  *Done:* implemented in `src/core/reload.rs` (GTK-free domain mapping — R6.2), with a new signal seam in `src/system/signal.rs`:
  - `ReloadAction` enumerates the distinct §6/apply-theme reloads — `HyprctlReload`, `EwwReload`, `SwayncReload`, `KittyColors` (SIGUSR1), `HyprpaperWallpaper` (expands to `hyprctl hyprpaper preload` then `wallpaper ,<path>`), `HypridleRestart`, `GsettingsSet { key, value }`, and `HyprctlSetCursor { theme, size }` — and `ReloadAction::execute` runs each through the seams, building its exact shell-free arg vector (no interpolation) and logging the reload + result (R7.3). Failures return a `ReloadError` but are non-fatal here; ordering/surfacing is the Apply pipeline's job (task 4.5, R5.5).
  - File → actions mapping (`BackingFile::reload_actions`, gathered by `plan_reloads`): `monitors.conf`/`input.conf`/`hyprland.conf` → `hyprctl reload`; `hypridle.conf` → hypridle restart; `hyprpaper.conf` → the hyprpaper action; `swaync/config.json` → `swaync-client -rs`; the GTK `settings.ini` files + `uwsm/env` → the cursor/theme `gsettings set` (+ `hyprctl setcursor`); a palette switch → the broad apply-theme chain (`hyprctl reload`, `eww reload`, `swaync-client -rs`, kitty SIGUSR1) in that order; and — critically — `hyprlock.conf` → **no reload** (it reads config only at launch, intentional). `plan_reloads` dedups a change that touches several files sharing a reload (e.g. the cursor's `settings.ini`/`uwsm/env`/`hyprland.conf` copies) and orders them canonically. Parameterized actions draw the wallpaper path / cursor theme+size / GTK+icon theme names from a `ReloadParams` the pipeline fills only for values that changed.
  - Capability gating (R4.2/R5.5, `ReloadAction::is_available`): hyprctl-family actions require `hyprland_reloadable()` (hyprctl + live IPC), the hyprpaper action additionally requires the hyprpaper daemon live; `eww`/`swaync`/kitty/hypridle require their daemon live; `gsettings set` requires the `gsettings` binary — so a reload is emitted only for a changed component that is present and running.
  - Kitty-signal seam: the new `ProcessSignaller` trait in `system/` (real `SystemProcessSignaller` scans procfs for matching PIDs and delivers the signal via `nix::sys::signal::kill` — no `pkill`, no shell; `MockProcessSignaller` records `(process, signal, PIDs)` and can inject an enumeration failure), used for kitty SIGUSR1 and the hypridle-restart fallback. The hypridle restart is `is-active`-gated: `systemctl --user is-active --quiet hypridle` decides the path (exit 0 ⇒ `systemctl --user restart hypridle`; else fallback) — deliberately not `try-restart`, whose exit 0 on an inactive-but-present unit (systemd #34192) would silently skip the fallback. The fallback (the target dotfiles run hypridle as an `exec-once`, not a unit) sends SIGTERM to the running hypridle and respawns it with `setsid --fork hypridle` through the normal `CommandRunner::run`, so `setsid` (which forks the daemon into a new session and exits at once) is reaped with no leaked zombie while hypridle reparents to init and survives app exit. (Since the task-6.9 review this respawn runs as a *detached* command — output discarded rather than captured — because the respawned daemon inherits capture-pipe write ends, and a capturing run would sit draining them until the daemon exits, stalling the Apply.)
  - Sync note: the palette reload chain mirrors `scripts/apply-theme` (analysis §6.1) and must be kept in step with it. Unit tests use `MockCommandRunner` + `MockProcessSignaller` and a `Capabilities::for_tests` constructor to assert each changed file maps to exactly the expected action list (order included), that actions drop when a capability is absent/daemon stopped, that `hyprlock.conf` alone yields no reload, that the kitty seam records the intended PIDs, and the exact arg vectors of every subprocess reload.

- [x] **4.5 Apply pipeline orchestrator** — Complexity: 4
  Fixed order: validate all → conflict check → atomic writes with per-file rollback → reloads for changed+running components only, via the 4.4 table (architecture §6). Palette change = run the discovered `scripts/generate-colors <scheme>` as the **last** write step (non-zero exit = write failure — which now also covers a missing/incomplete `theme/fonts`, since generate-colors depends on it, analysis §6.5), so rollback of earlier files never leaves the generated files (six color + three font partials + marker) on the new scheme. Each write logged with path + changed keys (R7.3). Reload failures non-fatal, logged `error`, surfaced (R5.3–R5.5). *Accept:* integration tests with mock runner assert exact command sequence per change class and failure-injection behavior, including that generate-colors runs after all file writes.
  *Done:* implemented in `src/core/apply.rs` (GTK-free orchestrator — R6.2), the capstone tying together the writer (2.2), freshness (2.3), model (4.1), detection (4.3), and reload table (4.4):
  - `apply::run` executes the fixed order as a chain of gates (architecture §6): (1) validate every dirty staged value via `SettingId::validate`, aborting before any write on failure (R8.3); (2) conflict-check the tracked target files via `FreshnessTracker::check_conflicts`, aborting before any write if one changed externally so an edit is never clobbered (R5.6); (3) write each planned file through `write_atomic`; (4) run the palette `generate-colors <scheme>` **last** among the write steps; (5) reload only the changed+running components via `plan_reloads` + `execute`, gated on `Capabilities`. Returns a structured `ApplyOutcome` (`ValidationFailed`/`Conflicted`/`WriteFailed`/`Applied { reload_failures, written }`) the UI (task 5.3) surfaces.
  - Commit contract: the pipeline is a pure orchestrator and never touches the store, so `Applied` reports the **live** paths written and the caller must then call the new `SettingsStore::commit_apply(&[(path, bytes)])` — it promotes staged→original and re-baselines those files' freshness from the exact bytes written (via `record_bytes`), so the app's own write is not mistaken for an external conflict on the next apply. Documented in both the `apply` and `store` module docs.
  - Rollback (R5.4): each successful `write_atomic` yields a `FileSnapshot` collected in write order; any write-phase failure — a file write **or** a non-zero/unrunnable `generate-colors` (the non-zero path also covers a missing/incomplete `theme/fonts`, since the generator aborts on it) — restores every already-written file from its in-memory pre-apply snapshot, leaving the desktop as it was. Ordering `generate-colors` last guarantees a file-write failure never runs it, so a rolled-back apply never strands the generated files (six color + three font partials + marker) on the new scheme.
  - Reloads are **non-fatal** (R5.5): a failed reload does not roll back the writes (they stand), is logged at `error` and collected into `reload_failures`, and the remaining reloads in the plan are still attempted. Each write is logged with its path + changed-key labels, never contents (R7.3).
  - The parser-agnostic seam: `run` takes an `ApplyPlan` (`validations`, `writes`, optional `palette`, `reload_params`) a page assembles by rendering its staged edits through the format parsers (§3) into complete file bytes; the pipeline only writes/reloads/rolls-back and derives the changed-`BackingFile` reload set from the writes plus a palette switch. In-crate `#[cfg(test)]` tests use `MockCommandRunner`/`MockProcessSignaller` + `tempfile` files to assert the exact command/arg sequence per change class (display → `hyprctl reload` only; palette → `generate-colors` then the apply-theme chain) and every failure-injection path (validation, conflict, mid-write rollback, `generate-colors` failure rollback, non-fatal reload with others still attempted), including that `generate-colors` runs after all file writes.

## 5. UI shell

- [x] **5.1 Main window: sidebar + stack** — Complexity: 2
  `GtkStackSidebar` + `GtkStack` with the seven categories; usable at 1.33-scaled 2880×1800 and 1440p (R2.4). Categories with zero visible rows hidden (R4.2). No libadwaita — it hard-codes Adwaita styling and ignores `gtk-theme-name` (R2.1/R2.2). *Accept:* window renders all detected categories; no custom CSS anywhere (R2.1).
  *Done:* implemented in `src/ui/window.rs` + `src/ui/category.rs`, populating the window `src/ui/app.rs` (task 1.3) creates:
  - The window's child is a horizontal `GtkBox` holding a `GtkStackSidebar` bound to a `GtkStack`, one page per visible category, defaulting to `960×640` with the stack set `hexpand`/`vexpand` and every page wrapped in a `GtkScrolledWindow`, so the sidebar-plus-scrollable-content layout stays usable at a 1.33-scaled 2880×1800 panel (~2165×1353 logical px) and at 1440p (R2.4). Each page is a placeholder (a titled, scrollable box) that the row framework (5.2) and the §6 pages plug into.
  - The seven categories (Display, Sound, Theme, Input, Notifications, Power & Idle, Network) live in a `SidebarCategory` enum, and `visible_categories(&Capabilities)` is a pure, GTK-free function — unit-tested headlessly (R6.2) — that decides which to show: a category is added to the stack/sidebar only when its gating capability is present, so one with no available content is cleanly hidden rather than greyed out (R4.2). Theme aggregates its four features (palette source, `gsettings`, `hyprpaper`, `hyprlock`) and is hidden only when all are gone. Row-level hiding + real content arrive with 5.2/§6.
  - No libadwaita and no custom CSS: only plain GTK4 widgets, with layout done via widget properties (expand/align/margin), inheriting the system GTK theme (R2.1/R2.2). A new grep guard `tests/no_custom_css.rs` (mirroring `tests/module_boundaries.rs`) fails if `src/` references `CssProvider`/`load_from_data`/`load_from_string`/`add_provider`, confirmed to catch an injected violation.
  - Verified live on the target Hyprland/Wayland session: the app enters the GTK main loop and maps a window with no errors (`hyprctl clients`: class `org.settings4000.Settings4000`, title `Settings4000`, non-zero size); detection found 11/12 binaries (only `swaync` absent), so the sidebar renders six of the seven categories with Notifications hidden — a live R4.2 demonstration. (A pixel-level screenshot of the content could not be captured cleanly — region grabs hit the wallpaper and a full-output grab coincided with the session's idle lock — so the render is evidenced at the window-manager level.)

- [x] **5.2 Declarative row framework** — Complexity: 3
  Row descriptor (`label`, widget kind, `SettingId`, required capability) → widget construction: `GtkDropDown`/`GtkSwitch`/`GtkScale`, plus a `GtkListBox`-backed reorderable editable list (add/remove/reorder rows) for ordered multi-value settings (R2.3). Widgets emit `SetValue` messages and render from store state only. *Accept:* a page defined purely as descriptor list renders and round-trips values through the store.
  *Done:* implemented in `src/ui/row.rs` (pure, headless framework) + `src/ui/page.rs` (the Relm4 page component), wired into `src/ui/window.rs`:
  - `RowDescriptor` (`label`, `WidgetKind`, `SettingId`, `RowCapability`) drives widget construction in a thin Relm4 `SimpleComponent` (`SettingsPage`): `GtkDropDown` → `Value::Enum` (over a fixed option set), `GtkSwitch` → `Value::Bool`, `GtkScale` → `Value::Float`/`Value::Integer` over a documented range/step (integer settings rounded, so a timeout never gains a fractional part), and a `GtkListBox`-backed reorderable editable list — add via a candidate drop-down, remove/reorder via per-row Up/Down/Remove buttons (buttons chosen over drag for a deterministic, dependency-free interaction) — editing an ordered multi-value setting as a comma-joined `Value::String` (e.g. keyboard layouts, task 6.6). The value↔widget conversions, widget/setting kind compatibility, gating, and the list add/remove/reorder ordering math (pure `list_with_*`/`list_without` helpers that return the value to emit, or nothing for a no-op) all live in the GTK-free `row.rs` and are unit-tested (R6.2). Added `SettingId::KeyboardLayouts` to `core/model.rs` as the R2.3 list example.
  - Thin ui / store round-trip: each control, on user change, emits a `SetValue { id, value }` (`PageMsg::Set`) that the component relays to `SettingsStore::stage` (→ `StageOutcome::Staged`, or `RuntimeBypass` for a runtime-only setting, R5.1/R5.2); `update_view` then re-renders **every** control purely from `store.value(id)`, so a store change from any source (a sibling edit, a reset, a reload) re-renders the widget. Scalar controls block their change signal around the programmatic set to stop a render being mistaken for a user edit; the reorderable list rebuilds only when the stored value differs, and a no-op list edit emits nothing. An invalid edit (R8.3) is rejected and the control snaps back to the stored value. Boundary: because a control's value is `store.value(id)`, the framework renders **file-backed settings only** — a runtime-only setting has no store value, so its control is built by bespoke §6 glue that renders from the live runtime source (a debug assertion in `build_control` enforces this), and the `RuntimeBypass` arm currently only logs, with the runtime-apply relay deferred to §6.1/§6.2.
  - Per-row capability gating (R4.2) via `RowCapability` + `visible_rows`, composing with 5.1's category gate in `window.rs`: each individually hidden row is logged at `info`; a category 5.1 shows whose rows are all gated out is dropped (`CategoryContent::Emptied`, logged at `info`), while a category with no framework rows yet keeps its 5.1 placeholder (`NoSpec`). Interim per-category descriptor lists (Input, Notifications) exercise all four widget kinds end-to-end; §6 supplies the full content and task 5.4 replaces the seeded originals with values parsed from the real config files.
  - Verified: `cargo fmt --check`, `cargo clippy --all-targets -D warnings` (clippy 1.97), and `cargo test` (293 unit + the two grep guards) all green; covered headlessly with the real store — the `SetValue`→`stage`→`value` round-trip, the runtime-only bypass, the invalid-edit revert, the list-edit helpers (add/remove/reorder incl. no-op → no emit and duplicate-add ignored), the per-row `visible_rows` gating, and `build_category`'s `NoSpec`/`Emptied` composition decisions. Ran live on the target Hyprland/Wayland session: the descriptor-driven Input page maps and builds with no errors (only benign GTK-internal `GtkDropDown` baseline warnings on gtk 4.22.4), and Notifications is hidden because `swaync` is absent (a live R4.2 demonstration). A pixel screenshot could not be captured — the session idle-lock caught the grab and fractional scaling offsets region grabs — so the render is evidenced at the window-manager level, as in task 5.1.

- [x] **5.3 Apply/Reset chrome + dirty indicators** — Complexity: 2
  Suggested-action Apply button, per-page dot markers, Reset, non-fatal error toasts for reload failures, conflict warning dialog on focus/refresh (R5.1, R5.5, R5.6); manual detection-refresh action that re-runs `core/detect` and repopulates pages (R4.3). *Accept:* dirty markers track store state; Reset clears; toast shown on injected reload failure.
  *Done:* implemented in `src/ui/chrome.rs` (new) with the wiring in `src/ui/window.rs`; `src/ui/page.rs` moved to the shared store:
  - The window now owns a single `SettingsStore` (behind `Rc<RefCell<…>>`) that every page shares, so an edit on any page lights up the chrome. A `HeaderBar` carries a **Reset** button and a suggested-action **Apply** button — both enabled only while `store.is_dirty()`, Apply styled with the built-in `add_css_class("suggested-action")` theme class (not custom CSS, so it does not trip `tests/no_custom_css.rs`) — plus a **Refresh** button. Pages report each edit through a shared `on_changed` callback so the chrome re-derives immediately; the window drives them back with a `PageMsg::Rerender` broadcast after a Reset/commit/reload.
  - Per-page dirty markers use the natural GTK mechanism: each `GtkStack` page's `needs-attention` property is set from `store.is_category_dirty(category)` (mapped from the sidebar category by `chrome::marker_category`), which `GtkStackSidebar` renders as a marker; they track staging and clear on Reset/Apply.
  - **Reset** calls `store.reset()`, re-renders all pages, and refreshes the chrome (markers clear, controls revert). **Apply** runs the real `apply::run` pipeline and dispatches the `ApplyOutcome` through the pure `chrome::respond_to_apply`: `Applied` → `SettingsStore::commit_apply` with the plan's written paths+bytes (promote staged→original **and** re-baseline freshness so the app's own write is not a self-conflict on the next Apply — the 4.5 commit contract), with a non-fatal reload-failure toast (R5.5); `ValidationFailed`/`Conflicted`/`WriteFailed` → a modal warning, no commit. The `ApplyPlan` is interim (validations only, no parser-produced file writes yet — that glue is §6).
  - The non-fatal toast is plain GTK4 with **no libadwaita**: a `GtkOverlay` over the content holding a `GtkRevealer` (label + Dismiss button, built-in `osd` style class) that slides in and auto-hides. The conflict warning is a plain modal `gtk::Window` (not the deprecated `MessageDialog`, not `adw`), shown when `apply::run` returns `Conflicted` or when `store.refresh()`'s `RefreshReport` reports externally-changed/failed files on a manual refresh (R5.6), rendered from the report's reloaded-vs-failed paths.
  - The Refresh button re-runs `core::detect` (rebuilds `Capabilities`) and repopulates the sidebar/stack so categories/rows appear or disappear (R4.3), and re-reads the tracked files for the conflict check. The shared store persists across a rebuild, so staged edits are kept; it is seeded once (interim) against a real temp file so `refresh` behaves until task 5.4 loads real configs.
  - Tested headlessly (R6.2): the pure chrome decisions in `chrome.rs` — dirty→Apply/Reset-enabled and per-page markers, Reset clearing dirty (real store); `ApplyOutcome`→commit/toast/warn incl. a reload-failure→toast and each abort→warn; and `RefreshReport`→conflict-warning content (real `store.refresh()`). Ran live on the target Hyprland/Wayland session: detection completed, the shared store seeded, and the window mapped with the header chrome and no panics (only the benign GtkImage baseline warnings).

- [x] **5.4 Startup sequencing** — Complexity: 3
  Detection + all file parsing on worker thread concurrent with window construction; pages populated on completion; detection summary logged at `info` (architecture §8). *Accept:* pages populate on worker-thread completion; missing optional apps never block startup (R4.3). (The < 500 ms budget, R8.1, is verified by task 7.3.)
  *Done:* implemented in `src/ui/startup.rs` (new) + a reworked `src/ui/window.rs`, with `src/ui/app.rs` reduced to delegating to `window::build`:
  - Worker-thread detect + parse: `startup::load` runs `core::detect` **and** parses the real backing config files — `~/.config/hypr/input.conf` (hyprlang → the Input page's keyboard layouts / mouse sensitivity / touchpad toggles) and `~/.config/swaync/config.json` (JSON → notification position + timeout) at their live XDG paths — into a plain `StartupLoad { capabilities, files }`. It runs on relm4's blocking pool (`relm4::spawn_blocking`) concurrently with the main thread assembling the window shell; the result is delivered back over a `relm4::channel` and awaited on the main thread (`relm4::spawn_local`), so GTK is only ever touched on the main thread. The pure load logic is GTK-free and headlessly tested (parse-into-values per file, missing/unparseable-file degradation, and store population + baseline).
  - Populate on completion: `build` returns immediately with a **persistent shell** (titlebar + chrome + an empty stack showing a loading spinner). The titlebar is set once before the window is realized and only the stack's category pages are (re)populated — so there is no `set_titlebar`-on-realized-window warning. When the worker delivers, `Shell::apply_startup_load` feeds each parsed file to `store.load_file` (establishing real `original` values + freshness baselines) and builds the sidebar/pages from the detected `Capabilities`. This replaces 5.3's synchronous interim seed against a temp file; the `demo_original`/`interim_seed_values` scaffolding is removed.
  - Non-blocking / degrade (R4.3/R4.4): detection degrades every absent probe to "absent", and a missing/unreadable/unparseable config is skipped with a `warn` (never its contents, R7.3) — one bad file never aborts the others, and a worker that fails to deliver still yields a working (empty) window with a Refresh; a setting whose file/key is absent simply renders its default and rejects edits until the file appears.
  - Info summary (architecture §8): detection logs its own one-line summary, and `apply_startup_load` adds a startup summary at `info` — the files-loaded count plus the visible-category count and titles, never file contents.
  - Apply tracker wiring: added `SettingsStore::freshness()` and wired the store's real tracker into `apply::run` (replacing the interim empty tracker), with a headless test that a second Apply after commit is not a self-conflict. The interim write-nothing `ApplyPlan` (real parser-produced file writes) is left to §6, where each page defines its own file/key/reload mapping.
  - Verified: `cargo fmt --check`, `cargo clippy --all-targets -D warnings` (clippy 1.97 = CI), and `cargo test` (309 unit + the `module_boundaries`/`no_custom_css` guards) all green. Ran live on the target Hyprland/Wayland session: the window maps immediately and, when the worker completes, the store loads both config files and six of seven categories render — Notifications hidden because `swaync` is absent (a live R4.2/R4.3 demonstration) — with the `info` summary logging `files_loaded=2, categories=6`.

## 6. Category pages (each: descriptor list + page-specific glue; depends on §3–§5)

- [x] **6.1 Display page** — Complexity: 4
  Per-monitor resolution/refresh/scale/position drop-downs + per-monitor enable switch (R2.3) from `monitors.conf` records + current `hyprctl monitors -j` state; laptop-display toggle drives the existing hotplug mechanism — writes/removes `/tmp/hypr-laptop-display-forced` and triggers the same path as `scripts/hypr-monitor-hotplug`, applied immediately (R5.2), never a `monitor=…,disable` record. `monitors.conf` is the single source for eDP mode/scale — `scripts/hypr-display-profile.sh` derives its values by parsing it (analysis §6.2), so a `monitor=` record edit must stay awk-parseable (leading `key,` token, mode field 2, scale field 4, extras after) and the app never writes the script. *Accept:* staged edit → Apply rewrites only the target `monitor=` record and runs `hyprctl reload`; the rewritten record is still parseable by `hypr-display-profile.sh`'s awk; toggle creates/removes the state file and never touches `monitors.conf`; page hidden and logged at `info` when `hyprctl` is absent (R4.2/R4.4).
  *Done:* implemented in `src/core/display.rs` (new, GTK-free model) + `src/ui/display.rs` (new, bespoke page glue), wired into `src/ui/window.rs` and `src/ui/startup.rs`:
  - Per-monitor identity: monitors are discovered at runtime, but `SettingId` (task 4.1) is a fixed fieldless enum, so per-monitor settings do not fit the `SettingsStore`. The Display page uses a self-contained `core::display::DisplayModel` — a page-local staging model mirroring the store's `original`/`staged`/dirty pattern but keyed by monitor — rather than forcing a dynamic key through the store. Its `monitors.conf` write is folded into the same `apply::run` (task 4.5) the window already drives, reusing the write/rollback/reload machinery without bending the store around dynamic keys; the window ORs the model's dirty state into the shared Apply/Reset chrome and drives the Display page's per-page marker from it.
  - The merge: `DisplayModel::from_sources` pairs each live output from `hyprctl monitors -j` (which outputs exist, their available modes/refresh rates for the drop-downs, and their current mode/scale/position/enabled state) with the configured values of the matching `monitor=` record (matched by exact name or `desc:` substring, honouring Hyprland's later-rule-wins order). The resolution/refresh/scale/position drop-downs + a per-monitor enable switch (R2.3) render from this; an output with no specific record still shows (editing appends one), and an unreadable `monitors.conf` hides the file-backed rows while keeping the laptop toggle (R4.4).
  - Real ApplyPlan write (the app's first): `apply_contribution` renders the staged edits through the task-3.3 parser — `set_field` for in-place mode/scale/position (extras-preserving) and `set_state` to (re)enable or first-configure a record — so Apply rewrites **only** the target `monitor=` record, keeps it awk-parseable (name field 1, mode field 2, scale field 4, extras after) for `hypr-display-profile.sh`, and reloads via `hyprctl reload` through the task-4.4 table; validations reuse the `MonitorMode`/`MonitorScale` validators (R8.3). The window commits the staged edits and updates the in-memory records after `Applied`.
  - Conflict safety (R5.6): `DisplayModel` owns a `FreshnessTracker` baselined from the exact `monitors.conf` bytes read at load; the window conflict-checks it before writing a dirty monitor edit and, on an external change, warns and re-loads rather than clobbering the stale parse, and `commit` re-baselines from the just-written bytes so the app's own write is not a self-conflict on the next Apply.
  - Runtime-only laptop toggle (R5.2): mirroring the dotfiles' own `scripts/hypr-toggle-laptop-display`, the internal panel's enable toggle applies the on/off state live with the granular `hyprctl keyword monitor "<output>,disable"` / `"<output>,<record body>"` command — **not** `hyprctl reload`, which re-reads the still-active record and so cannot turn the panel off — and creates/removes `/tmp/hypr-laptop-display-forced` (the watcher's manual override) at an injectable path. It is applied immediately, never staged, never dirty, never a `monitor=…,disable` record, and never touches `monitors.conf`; the panel's mode/scale are still staged record edits (the single-source gotcha). The `hyprctl monitors -j` probe and the state-file path are injectable so tests need no live Hyprland.
  - Gating (R4.2/R4.4): Display stays gated on `hyprctl` by task 5.1's category gate; when the compositor is not reloadable the worker builds no model and the page degrades cleanly to a placeholder (logged at `info`), and an unreadable `monitors.conf` degrades to hiding the file-backed rows (logged at `warn`).
  - Verified: `cargo fmt --check`, `cargo clippy --all-targets -D warnings` (clippy 1.97 = CI), and `cargo test` (331 unit + the `module_boundaries`/`no_custom_css` guards) all green. New headless tests cover the record↔`hyprctl monitors -j` merge (canned JSON via `MockCommandRunner`); the staged-edit → surgical awk-parseable `FileWrite` + `hyprctl reload` sequence; the record-less first-configure (append) and disabled→re-enable (mode/scale sourced from live) paths; the monitors.conf conflict-check detecting an external edit and a committed apply not self-conflicting; and the laptop toggle issuing the granular `hyprctl keyword monitor` on/off command (never `reload`) + state-file create/remove not touching `monitors.conf`. Ran live on the target Hyprland/Wayland session: the window maps and the Display page builds from the real eDP-1 merge among six visible categories (Notifications hidden — swaync absent), no panics (only the benign GtkImage baseline warnings seen since task 5.3).

- [x] **6.2 Sound page (runtime-only)** — Complexity: 3
  Output/input device drop-downs (switching via `wpctl set-default <id>`), volume sliders, mute switches driving `wpctl` immediately (R3.1, R5.2); device/state enumeration on page entry from `pw-dump` JSON, falling back to parsing `wpctl status`. *Accept:* controls bypass staging; device switch issues `wpctl set-default` with the right id; commands verified via mock runner; page hidden when `wpctl`/`pactl` absent.
  *Done:* implemented in `src/core/sound.rs` (new, GTK-free model) + `src/ui/sound.rs` (new, bespoke page glue), wired into `src/ui/window.rs`:
  - Entirely runtime-only (R3.1/R5.2): every control applies **immediately** by running a `wpctl` command. The page stages nothing, is never dirty, never touches the `SettingsStore`, and has no Apply/Reset or dirty-marker involvement — so it deliberately does not use the store-driven task-5.2 row framework (which is file-backed only). Like the Display page (task 6.1) it is a bespoke GTK-free `core` model plus bespoke `ui/` glue, mirroring that split.
  - Enumeration on page entry: `core::sound::enumerate` runs `pw-dump` and parses the PipeWire object dump — the `Audio/Sink`/`Audio/Source` nodes, the current defaults from the `default` metadata object (`default.audio.{sink,source}` → `node.name`), and each node's volume/mute — **falling back** to parsing `wpctl status` (Audio-section Sinks/Sources only, ignoring the Video section and the numeric config-default lists) when `pw-dump` is absent, exits non-zero, or emits unparseable output; if neither works it degrades to an empty list. Never panics. Volume is read on WirePlumber's **cubic** curve: `pw-dump`'s stored `channelVolumes` are cube-rooted to the `wpctl`-scale value the slider shows (verified empirically — `wpctl set-volume 0.5` stores `0.125`), while `wpctl status` already reports that scale.
  - The `wpctl` command set (shell-free arg vectors via `CommandRunner`): device drop-downs → `wpctl set-default <id>`; volume sliders → `wpctl set-volume <id> <v>` (the `wpctl`-scale value, which `wpctl` cubes); mute switches → `wpctl set-mute <id> 1|0`. The `ui/` page renders one output and one input section (device drop-down + volume slider + mute switch for the default device) from the enumerated state and calls the model's command on change; switching the default re-enumerates so the volume/mute rows follow, and a "Rescan devices" button plus re-showing the page re-enumerate to pick up external changes.
  - Sync-on-main-thread (deliberate): the `wpctl`/`pw-dump` calls are short-lived, so — matching the task-6.1 convention — they run synchronously on the GTK main thread via `CommandRunner::run` rather than on a worker; there is no staging pipeline to coordinate with, so no async is warranted.
  - Gating (R4.2): because the page's enumeration and controls speak only `wpctl`/`pw-dump`, Sound is gated on `has_binary(Binary::Wpctl)` specifically (not the broader `audio_available`, `wpctl` OR `pactl`) — a `pactl`-only host would otherwise show a dead, inert page, which R4.2 forbids, so it is hidden there; `pactl`-only support is out of v1 scope. The whole page is cleanly hidden when `wpctl` is absent (the hidden-item reason is logged at `info`, consistent with the rest).
  - Verified: `cargo fmt --check`, `cargo clippy --all-targets -D warnings` (clippy 1.97 = CI), and `cargo test` (344 unit + the `module_boundaries`/`no_custom_css` guards) all green. New headless tests (via `MockCommandRunner`) cover the `pw-dump` JSON parse (correct devices/defaults/cube-rooted volume/mute, first-channel for unequal multi-channel, absent-`Props` defaulting to full/unmuted, excluding video and non-node objects), the `wpctl status` fallback parse (Audio sinks/sources only), the `enumerate` source selection (pw-dump preferred; a valid but audio-free dump is authoritative and does not fall back; fall back on spawn failure and on garbled output; degrade to empty when both fail), the exact command arg-vectors, and that a device switch / volume / mute issues the exact `wpctl` command with the right id — with no store interaction (the module holds no `SettingId`); the Sound category is hidden on a `pactl`-only host (S1). Ran live on the target Hyprland/PipeWire session: the window maps among six visible categories with Sound shown (Notifications hidden — swaync absent, a live R4.2 demonstration) and enumerates the built-in audio sink read-only; the command builders were validated against `wpctl` using a throwaway null sink, so the user's real audio was never changed.

- [x] **6.3 Theme page — palette scheme** — Complexity: 2
  Drop-down over the palette files in the discovered repo's `colors/` dir (repo root resolved from a deployed config symlink, not a hardcoded `~/.dotfiles` path — R8.5; enumeration skips dotfiles, subdirectories, and non-palette files so a state marker never appears as a scheme) with active-scheme detection (task 3.7); degrades to read-only display when <2 schemes (R3.2); Apply runs the discovered `generate-colors` + reload chain via the pipeline. *Accept:* fixture with one scheme shows read-only; switch triggers exact expected command sequence; controls hidden and logged at `info` when the palette source (`generate-colors`/the colors dir, or the repo behind the config) is absent (R4.2/R4.4).
  *Done:* implemented in `src/core/theme.rs` (new, GTK-free `PaletteModel`) + `src/ui/theme.rs` (new, bespoke section-based page glue), wired into `src/ui/startup.rs` and `src/ui/window.rs`:
  - Scheme enumeration (R3.2/R8.5): `PaletteModel::load` enumerates the switchable schemes from the **discovered** repo's `colors/` dir — the dir comes from `Capabilities::palette_source()` (repo root canonicalized from the deployed config symlink, task 4.3), never a hardcoded `~/.dotfiles` path. An entry is a scheme only when it is not a dotfile, is a regular file (so subdirectories are excluded), and parses as a **schema-valid** palette (via task 3.7's swatch), which is what skips `README.md`, a `state/active-scheme`-style marker, or any other non-palette file — the same key-presence bar `generate-colors` uses, so every offered scheme is one it would accept. Results are sorted by name for a stable drop-down.
  - Active-scheme preselect (R3.2, task 3.7): the active scheme is read from the deployed generated `~/.config/hypr/colors.conf` header (`generated::read_active_scheme`) and preselected; `selected()`/`selected_index()` return the staged switch if one is pending, else the active scheme.
  - `<2` read-only degrade (R3.2): `is_switchable()` is true only with ≥2 schemes; with 0 or 1 the UI shows the active scheme as read-only text (no functional drop-down). A small Cairo `DrawingArea` preview strip draws the selected scheme's colors (task 3.7 swatch) — not CSS-colored widgets, so `tests/no_custom_css.rs` still passes.
  - Staging + Apply: the palette scheme is a **bespoke staging source** (like the Display model, not a `SettingId` in the store) because its "original" comes from a generated header the app never writes and its Apply produces no `FileWrite` — it contributes an `apply::PaletteSwitch { scheme, generate_colors }` into `ApplyPlan.palette` (NO `colors/<scheme>` write, per R3.2/§9). The window folds it into the shared Apply/Reset chrome + dirty marker (a pending switch lights up Apply and the Theme marker) and the same `apply::run` pipeline, so applying runs the discovered `generate-colors <scheme>` LAST and then the palette reload chain (`hyprctl reload` → `eww reload` → `swaync-client -rs` → kitty SIGUSR1); commit promotes staged→active, Reset discards.
  - User-facing label (5.3 review note): the control is labelled "Colour palette" (section frame) / "Scheme" (row) rather than a raw id.
  - Gating (R4.2/R4.4): the worker builds the model only when the palette source is present; when it is absent the Theme palette section is hidden (page degrades to the task-5.1 placeholder) and logged at `info`, while the Theme *category* still hides entirely only when all its OR-gated features are gone (task 5.1). Fixed a defect this surfaced: the palette parser (task 3.1) logged its warnings itself, flooding the journal when a `README.md` is probed during enumeration — it now *returns* warnings for the caller like the sibling parsers, and the swatch probe drops them.
  - Verified: `cargo fmt --check`, `cargo clippy --all-targets -D warnings` (clippy 1.97 = CI), and `cargo test` (353 unit + the `module_boundaries`/`no_custom_css` guards) all green. New headless tests cover scheme enumeration from a fixture `colors/` dir (skips a dotfile, a subdirectory, a `README.md`, and a `state/active-scheme`-style marker), active-scheme preselect, the `<2`-schemes read-only degrade (0/1/2 schemes), stage/dirty/reset/commit, and — via `MockCommandRunner` + `apply::run` — that a staged switch produces the exact `generate-colors <scheme>` + reload-chain sequence with no file write. Ran live on the target Hyprland/Wayland session (read-only, scheme NOT switched): the Theme page renders the palette drop-down among six visible categories with `schemes=2` (README.md skipped) and `active=everforest` preselected, no warning flood.

- [x] **6.4 Theme page — GTK/icon/cursor themes** — Complexity: 4
  Discovery scans (`~/.themes`, `~/.local/share/themes`, `/usr/share/themes` for gtk dirs; icon dirs with `index.theme`, cursor = has `cursors/`) (R3.3–R3.4). Apply = `gsettings` + both `settings.ini` + `hyprctl setcursor` + identical `XCURSOR_*` writes to `hyprland.conf` env and `uwsm/env`. `GTK_THEME` override (set in the app's own environment or uncommented in `uwsm/env`) shows a banner and disables the GTK drop-down (R3.3). *Accept:* discovery unit-tested against fixture trees; cursor apply writes both files to the same value; banner path tested; changing the GTK theme from the app restyles the app live when the portal/dconf path is active (R2.2) — verified manually on the target setup, with the "takes effect at next launch" fallback shown otherwise; GTK/icon/cursor controls hidden and logged at `info` when `gsettings` is absent (R4.2).
  *Done:* implemented as a new GTK-free `ThemesModel` in `src/core/theme.rs` (alongside `PaletteModel`) plus a new appearance section in `src/ui/theme.rs`, wired into `src/ui/startup.rs` and `src/ui/window.rs`:
  - Discovery (R3.3/R3.4): `discover_gtk_themes` collects theme dirs containing a `gtk-3.0/`/`gtk-4.0/` subdir; `discover_icon_and_cursor_themes` scans the icon roots in one pass with **independent** classification — a dir with a `cursors/` subdir is a cursor theme, and a dir with `index.theme` plus real icon content (a non-`cursors` subdir) is an icon theme, so a dir can be both. This deliberately deviates from R3.4's literal partition because real icon themes (GNOME's default Adwaita, Breeze/Oxygen) bundle a `cursors/` set; the "real icon content" gate still keeps pure cursor packs that ship an `index.theme` (Bibata-style) out of the icon list. Names are de-duplicated across roots and sorted. The scanned roots are an injectable `ThemeRoots` (resolved from `~/.themes`/`$XDG_DATA_HOME`/`/usr/share` in `startup`), so discovery is unit-tested against fixture trees.
  - Sections (added to the 6.3 section-based page): GTK-theme, icon-theme, cursor-theme drop-downs plus a cursor-size drop-down, each preselecting the current value read from `settings.ini` (cursor falling back to `uwsm/env`'s `XCURSOR_*`), each labelled (GTK theme / Icon theme / Cursor theme / Cursor size). Rows are hidden with a note when no `settings.ini` is readable (`themes_editable`, R4.4).
  - Multi-file write + reloads: a cursor change emits `FileWrite`s for **all four** copies — both `gtk-{3,4}.0/settings.ini` (3.5), `hyprland.conf`'s repeatable `env = XCURSOR_THEME/XCURSOR_SIZE` lines (3.2), and `uwsm/env` (3.6) — with the identical value, plus `ReloadParams` driving `gsettings set cursor-theme/-size` + `hyprctl setcursor` (and `hyprctl reload` from the hyprland.conf change); a GTK/icon theme change writes only the two `settings.ini` and `gsettings set gtk-theme`/`icon-theme`. Folded into the shared `apply::run` via `ThemesApply`; the model owns its own freshness baseline for the four files (conflict-checked before Apply, re-baselined on commit — R5.6), like the Display model.
  - `GTK_THEME` override (R3.3): `resolve_gtk_override` prefers a value set in the app's own environment (`std::env`) over an uncommented `uwsm/env` `GTK_THEME` (via 3.6 `gtk_theme_override()`); when either is active the appearance section shows a plain-GTK `Frame`+icon+label banner and disables the GTK-theme drop-down (icon/cursor stay editable), and staging a GTK theme is ignored — never fighting the override.
  - Live-restyle claim (R2.2): gated on `settings_portal_available()` — the section shows "restyles running apps immediately" when the portal/dconf path is present, else "takes effect the next time each app starts".
  - Gating (R4.2): the whole GTK/icon/cursor model is built (in `startup::load_themes`) only when `gsettings` is present; otherwise it is hidden and logged at `info`. The Theme page is built whenever either the palette or themes model exists, and its dirty marker/chrome OR-in the themes model.
  - Verified: `cargo fmt --check`, `cargo clippy --all-targets -D warnings` (clippy 1.97 = CI), and `cargo test` (371 unit + the `module_boundaries`/`no_custom_css` guards) all green. New headless tests cover discovery against fixture trees (gtk/icon/cursor with independent classification — Adwaita in both lists, a pure cursor pack with an `index.theme` staying cursor-only, dotfile/non-theme skipping, cross-root dedup); the cursor apply writing all four files to the same value with the exact `hyprctl reload`/`gsettings`/`hyprctl setcursor` sequence via `MockCommandRunner` + `apply::run`; the GTK-theme-only single-reload path; the `GTK_THEME` override (app-env and uwsm) → banner + disabled dropdown (pure + model); the settings-portal → live-vs-next-launch decision; gsettings-absent → model hidden; the cursor preselect falling back to `uwsm/env`; a committed apply not self-conflicting; a non-numeric on-disk cursor size still writing files but skipping the live cursor reload; a partial `hyprland.conf` write when one `env` line is missing; and the conflict-check/reload re-baseline. Ran live on the target Hyprland/Wayland session (read-only, no theme applied): the appearance section built with the portal detected (`settings_portal=true`, `live_restyle=true`, 25 gtk / 27 icon / 10 cursor themes, no override); the actual live GTK restyle on Apply is left to manual verification since applying would mutate the user's desktop, and gsettings/`settings.ini` were confirmed unchanged afterward.

- [x] **6.5 Theme page — wallpaper & lock background** — Complexity: 2
  Path choosers writing `hyprpaper.conf` and `hyprlock.conf` via the hyprlang editor, plus a `fit_mode` drop-down (same hyprlang edit); hyprpaper reload via `hyprctl hyprpaper …`. The two paths now default to the **same** image (analysis §6.2), so the UI presents a single wallpaper with an optional lock-screen override, still writing both files. hyprlock intentionally gets no reload — it reads its config at launch, so changes apply at the next lock. *Accept:* staged path/fit-mode edit produces exact expected file diff + preload/wallpaper commands; no reload command issued for hyprlock-only changes; wallpaper/lock controls hidden and logged at `info` when hyprpaper/hyprlock are absent (R4.2/R4.4).
  *Done:* implemented as a new GTK-free `WallpaperModel` in `src/core/theme.rs` plus a new wallpaper section in `src/ui/theme.rs`, wired into `src/ui/startup.rs` and `src/ui/window.rs` (with a `fit` field added to `ReloadParams`/`HyprpaperWallpaper` in the task-4.4 `src/core/reload.rs` so a fit change applies live):
  - Single wallpaper + optional lock override (analysis §6.2): the section shows **one** wallpaper image picker (a plain `gtk::FileDialog` — no libadwaita) plus a `fit_mode` drop-down (hyprpaper's `cover`/`contain`/`fill`/`tile`, with the on-disk value always kept selectable) and — only when hyprlock is present — a "use a different lock-screen image" toggle that reveals a second picker. With no override the **same** path is written to both files; with one, the wallpaper goes to `hyprpaper.conf` and the override to `hyprlock.conf`. The override starts on only when the two on-disk paths already differ (i.e. not the unified default).
  - Surgical hyprlang writes (task 3.2): a wallpaper/fit change edits only `wallpaper.path`/`wallpaper.fit_mode` in `hyprpaper.conf`; the lock background edits only `background.path` in `hyprlock.conf`. Folded into the shared `apply::run` via `WallpaperApply` (writes + reload params + validations), a bespoke staging source like the Display and GTK/icon/cursor models rather than a store `SettingId`.
  - hyprpaper reload vs hyprlock **no**-reload (architecture §6, task 4.4): a change touching `hyprpaper.conf` sets the wallpaper reload parameters (path **and** fit), driving `hyprctl hyprpaper preload <path>` then `wallpaper ,<path>,<fit>` — hyprpaper's IPC takes the fit as a third comma-field, so the fit applies live (a fit-only change re-sets the same image with the new fit) rather than only on the next hyprpaper restart. A change touching **only** `hyprlock.conf` issues no reload command at all — hyprlock reads its config at the next lock (intentional), enforced by the reload table (`HyprlockConf` maps to no action) and by setting the wallpaper parameters only when `hyprpaper.conf` is written.
  - Image validation (R8.3): a chosen path is validated with task 4.1's `validate_image_path` (exists + readable + image extension) **before** staging — an invalid path is rejected with a plain `gtk::AlertDialog` and never staged — and re-validated by the pipeline at apply time, so a path deleted between staging and Apply is caught before any write. A fit-only change re-validates no path (the unchanged wallpaper path is never rewritten).
  - Conflict-safety (R5.6) + gating (R4.2/R4.4): the model owns a `FreshnessTracker` baselined from the bytes read at load, conflict-checked before Apply (warn + re-load rather than clobber) and re-baselined on commit so the app's own write is not a self-conflict; the wallpaper section is built only when hyprpaper is present, the lock override only when hyprlock is, and the Theme category still hides entirely only when all its features are gone — every hidden reason logged at `info`. Enabled the gtk4 `v4_10` feature for `gtk::FileDialog`/`gtk::AlertDialog` (their deprecated predecessors would trip `-D warnings`).
  - Verified: `cargo fmt --check`, `cargo clippy --all-targets -D warnings` (clippy 1.97 = CI), and `cargo test` (383 unit + the `module_boundaries`/`no_custom_css` guards) all green. New headless tests cover the staged wallpaper + fit-mode edit → the exact `hyprpaper.conf`/`hyprlock.conf` `FileWrite` diffs *and* the `hyprctl hyprpaper preload`/`wallpaper ,<path>,<fit>` command sequence (via `MockCommandRunner` + `apply::run`), a fit-only change still reloading hyprpaper with the new fit, a hyprlock-only change issuing no reload command, the override-on branches via a distinct-on-disk-paths fixture (override starts on; a distinct override writes both files to two paths; turning the override off resyncs the lock to the wallpaper with no reload), an unusual on-disk `fit_mode` staying selectable, image-path rejection of a missing/non-image path, hyprpaper/hyprlock-absent gating, and reset + committed-apply (no self-conflict). Ran live on the target Hyprland/Wayland session (read-only, wallpaper NOT changed): the wallpaper section loaded from the real config (`wallpaper=true, lock=true, override_on=false, lock_available=true`) among six visible categories (Notifications hidden — swaync absent).

- [x] **6.6 Input page** — Complexity: 3
  Keyboard layouts as an ordered editable list (add/remove/reorder → `kb_layout=us,se`), entries sourced from the XKB registry (`/usr/share/xkb/rules/evdev.xml`); keyboard options as curated switches for the options in use (`caps:escape`, `grp:win_space_toggle`), preserving unknown `kb_options` entries verbatim; mouse sensitivity slider, touchpad switches (natural scroll, tap-to-click) editing the `input { }` section path in `config/hypr/input.conf` (the `source=`d file, **not** `hyprland.conf` — analysis §6.3) via the hyprlang writer. *Accept:* each control maps to the right section path; layout reorder round-trips; unknown `kb_options` survive an edit untouched; Apply diff limited to the touched lines in `input.conf`; `hyprctl reload` triggered; page hidden and logged at `info` when `hyprctl` is absent (R4.2/R4.4).
  *Done:* implemented in `src/core/input.rs` (new, GTK-free) + `src/ui/input.rs` (new descriptor glue), building on the task-5.2 store-driven row framework; wired through `src/ui/startup.rs` and `src/ui/window.rs`:
  - The general **store-`SettingId` → `FileWrite`** glue this task's big picture calls for (reused by Notifications, task 6.7): `SettingsStore::dirty_in_category` hands a page its dirty settings + values, and `core::input::render_input_conf` applies each to the current `input.conf` bytes through the surgical hyprlang writer (§3.2, section-path edits) to produce one lossless `FileWrite` whose diff is limited to the touched lines. The window's Apply folds this into the same `apply::run` pipeline — replacing the write-nothing interim for the Input category — and `hyprctl reload` follows (task 4.4, `BackingFile::InputConf`). Because `input.conf` is loaded through the store (which baselines it, whose tracker `apply::run` conflict-checks, and which `commit_apply` re-baselines from the written bytes), conflict-safety (R5.6) needs no bespoke tracker here; the `InputModel` reads the file fresh each Apply so repeated applies and external edits stay correct.
  - All five Input settings are ordinary file-backed store `SettingId`s edited by the row framework: keyboard layouts as the reorderable editable list (R2.3) serialising to `input.kb_layout`, mouse sensitivity as a slider over the 4.1 `SENSITIVITY_RANGE` → `input.sensitivity`, and the two touchpad switches → `input.touchpad.natural_scroll` / `input.touchpad.tap-to-click`. The layout list is validated to require at least one entry (R8.3): removing every layout is rejected on stage, so the control reverts rather than writing a bare `kb_layout=` that Hyprland would silently replace with its default. The layout add-list candidates are sourced from the XKB registry: `parse_xkb_layouts` extracts each base layout's `name`/`description` from `evdev.xml` (variants and models excluded, XML entities decoded); the registry path is injectable and resolved from the well-known locations (the requirements' `/usr/share/xkb/rules/evdev.xml` plus the FHS-standard `/usr/share/X11/xkb/rules/evdev.xml`), degrading to an empty add-list when absent (R4.4).
  - Keyboard options are the new `SettingId::KeyboardOptions` (task 4.1) holding the whole comma-joined `kb_options` string; a new `WidgetKind::TokenSwitch` renders one curated switch per option (`caps:escape`, `grp:win_space_toggle`) that toggles just its token via the pure `value_from_token_toggle`, so an option the app has no switch for is **preserved verbatim** through an edit (R4.2).
  - Gating (R4.2/R4.4): the Input category is `hyprctl`-gated by task 5.1, so the page is hidden entirely when `hyprctl` is absent (logged at `info` by detection); an unreadable `input.conf` degrades to controls with no store value that render defaults and reject edits.
  - Verified: `cargo fmt --check`, `cargo clippy --all-targets -D warnings`, and `cargo test` (406 unit + the `module_boundaries`/`no_custom_css` guards) all green. New headless tests cover the section-path map, a layout reorder round-tripping to `kb_layout=us,se`, an unknown `kb_options` surviving an edit untouched, a `kb_options` cleared to empty / appended when absent, the sensitivity and both touchpad edits touching only their own lines, the empty-layout rejection (on stage), the `TokenSwitch` toggle preserving unknowns, the XKB parse from a fixture `evdev.xml` (+ graceful absence), the dirty-edit → surgical `FileWrite` + `hyprctl reload` via `MockCommandRunner` + `apply::run`, the abort-on-unrenderable-write (read failure / missing section), and an end-to-end second-apply-after-commit not self-conflicting through the real renderer. Ran live on the target Hyprland/Wayland session (read-only, `input.conf` NOT changed): the window maps and the Input page builds among six visible categories (Notifications hidden — swaync absent), with the XKB layout list loaded from the X11 registry path.

- [x] **6.7 Notifications page** — Complexity: 2
  Position drop-down, timeout sliders, DND switch over `swaync/config.json`; reload via `swaync-client -rs`. *Accept:* JSON edits round-trip with stable key order; page hidden when swaync absent.
  *Done:* implemented in `src/core/notifications.rs` (new, GTK-free) + `src/ui/notifications.rs` (new DND glue), wired through `src/ui/startup.rs` and `src/ui/window.rs`, reusing the store-driven position/timeout rows that live in `src/ui/page.rs::category_rows`:
  - Position + timeout are ordinary store-backed `SettingId`s rendered by the task-5.2 row framework — a position drop-down covering swaync's full `positionY`×`positionX` grid (top/center/bottom × left/center/right = all nine combinations, so a live `center` config preselects) plus a timeout slider — so they reuse task 6.6's store-`SettingId` → `FileWrite` glue: `core::notifications::render_swaync_config` (mirroring `render_input_conf`) applies the store's dirty Notifications settings to `config.json` through the task-3.4 swaync adapter, and `NotificationsModel::swaync_config_write` folds one `FileWrite` (`BackingFile::SwayncConfig`) into the same `apply::run`, followed by the `swaync-client -rs` reload (task 4.4). `config.json` is store-loaded, so conflict-safety rides the store's tracker (no bespoke tracker) and the write is captured into `commit_apply`'s re-baseline set like the Input write.
  - The position **decomposes** on write: the store's combined `"<positionY>-<positionX>"` enum token (composed by the startup loader) splits on the first hyphen back into the two on-disk keys `positionY` + `positionX`; the timeout sets the integer `timeout` key. A JSON edit changes only those keys and preserves key order (task-3.4 `preserve_order`) — verified byte-for-byte against the fixture.
  - **DND is RUNTIME-ONLY** (per the recorded 3.4 gotcha): swaync's do-not-disturb is live daemon state, **not** a `config.json` key (the real config has only a `widgets` entry + a `widget-config.dnd` label, no top-level `dnd` boolean), so setting a `dnd` key would append a dead key swaync ignores. The DND switch is bespoke glue like the Sound page's controls: it reads the current state with `swaync-client --get-dnd` and applies changes immediately with `swaync-client --dnd-on` / `--dnd-off` (flags confirmed against `swaync-client(1)`), on the main thread, bypassing staging entirely — never staged, never dirty, no config key written, re-queried on page entry. `render_swaync_config` only ever touches position/timeout (a test asserts it never introduces a `dnd` key).
  - Gating (R4.2/R4.4): the Notifications category is `swaync`-gated by task 5.1, so the whole page (framework rows + DND switch) is hidden and logged at `info` when swaync is absent. The rows carry `RowCapability::Binary(Swaync)` (swaync **binary** presence, not config readability); following the Input-6.6 pattern, when swaync is present but `config.json` is missing/unreadable the rows still build and simply render their defaults, rejecting edits until the file appears (R4.4).
  - Verified: `cargo fmt --check`, `cargo clippy --all-targets -D warnings` (clippy 0.1.97 = CI), and `cargo test` (424 unit + the `module_boundaries`/`no_custom_css` guards) all green. New headless tests (via `MockCommandRunner` + `apply::run`) cover the position decompose to `positionY`+`positionX` (including the `center` vertical half) and the timeout integer key as JSON round-trips changing only those keys with stable key order, the drop-down offering all nine swaync positions with a `center-*` value preselecting, the full dirty edit → `FileWrite` + `swaync-client -rs` reload, a second apply after commit not self-conflicting through the real renderer, that the DND toggle issues the exact `swaync-client --dnd-on`/`--dnd-off` command and is neither staged nor writes a config key, and swaync-absent → the model/page is hidden. Ran live on the target Hyprland/Wayland session (read-only): the window maps among the visible categories with Notifications hidden — swaync absent — logging the R4.2 hidden reason; the DND live path could not be exercised (no swaync) and the user's DND/reload were not touched.

- [x] **6.8 Power & Idle page** — Complexity: 3
  Dim/lock/dpms timeout sliders + lock command entry over `hypridle.conf` listener/general blocks (hyprlang writer, positional listener matching); hypridle restart on Apply. *Accept:* editing one listener's timeout leaves the other listener blocks byte-identical; restart command issued only when the file changed; page hidden and logged at `info` when hypridle is absent (R4.2/R4.4).
  *Done:* implemented in `src/core/power.rs` (new, GTK-free) as a fully store-driven page — four framework rows built through the generic `page.rs`/`row.rs` path with no bespoke page glue — wired through `src/ui/startup.rs` and `src/ui/window.rs`:
  - The store-`SettingId` → `hypridle.conf` write glue mirrors the 6.6/6.7 shape: `core::power::render_hypridle_conf` applies the store's dirty Power & Idle settings to the current bytes through the surgical hyprlang writer (§3.2), and `PowerModel::hypridle_conf_write` folds one `FileWrite` (`BackingFile::HypridleConf`) into the same `apply::run`. `hypridle.conf` is store-loaded (added to `startup.rs::load_files` with a `load_hypridle_conf` loader), so conflict-safety rides the store's tracker and the write is re-baselined by `commit_apply` like the Input/swaync writes (R5.6).
  - **Positional listener matching** (the headline criterion): the shared `power_key_path` map — used by both the loader and the writer — addresses the three timeouts by `listener` occurrence via the 3.2 `SectionStep::nth`: `listener[0].timeout` = dim, `listener[1].timeout` = lock, `listener[2].timeout` = DPMS, per the real config's order (analysis §6, documented as the load-bearing assumption). Editing one listener's timeout leaves the other listener blocks byte-identical (occurrence-indexed surgical edits). The lock command edits `general.lock_cmd` — the command hypridle actually runs to lock — verified against the live file (not `listener[1]`'s `loginctl lock-session` on-timeout).
  - hypridle restart **only when `hypridle.conf` changed**: the pipeline derives its reload set from the writes, so a dirty edit yields a `HypridleConf` change → the 4.4 `HypridleRestart` (the `is-active`/`systemctl restart` else SIGTERM + `setsid --fork` mechanism), while a clean Power & Idle page produces no write and no restart.
  - The three timeout `SettingId`s (`DimTimeout`/`LockTimeout`/`DpmsTimeout`, kind Integer, category Power & Idle, file-backed, validated against `TIMEOUT_SECONDS_RANGE`) plus the pre-existing `LockCommand` were already coherent in 4.1's exhaustive model and test scaffolding; the sliders reuse `WidgetKind::Scale` and the lock command uses a new minimal `WidgetKind::Entry` (a `GtkEntry` rendered only when its text differs from the store, so a render never resets the cursor mid-edit). The lock command is validated at **edit** time — `SettingId::validate` now rejects a `#`/newline (`validate_command`, R8.3), mirroring the hyprlang writer's guard — so the Entry reverts on the offending keystroke rather than only failing the whole Apply; the writer keeps its guard as defense-in-depth.
  - Gating (R4.2/R4.4): the whole category is `hypridle`-gated by task 5.1, so the page is hidden and logged at `info` when hypridle is absent; rows carry `RowCapability::Binary(Hypridle)` and, following the 6.6/6.7 pattern, an unreadable `hypridle.conf` degrades to controls rendering defaults and rejecting edits. Verified `cargo fmt --check`, `cargo clippy --all-targets -D warnings`, and `cargo test` (441 unit + the `module_boundaries`/`no_custom_css` guards) all green, with headless tests covering the positional edits (one listener changed, others byte-identical), each control's address, the lock-command edit, the dirty edit → `FileWrite` + hypridle restart via `MockCommandRunner` + `apply::run`, no restart when unchanged, and a second-apply-after-commit not self-conflicting. Ran live on the target Hyprland session (read-only, `hypridle.conf` NOT changed, daemon NOT restarted): the window maps with Power & Idle among six visible categories (Notifications hidden — swaync absent) and `hypridle.conf` loaded into the store.

- [x] **6.9 Network page (read-only)** — Complexity: 2
  Connection status from `nmcli -t` + "Open Network Settings" button spawning `kitty -e nmtui` (gated on the kitty capability) or `nm-connection-editor` when installed (R3.1). *Accept:* status renders; button spawns the expected command per available capability (mock runner); page hidden without NetworkManager.
  *Done:* implemented in `src/core/network.rs` (new, GTK-free) + `src/ui/network.rs` (new, bespoke page glue), wired into `src/ui/window.rs`, with `Binary::NmConnectionEditor` added to `src/core/detect.rs`:
  - Read-only and runtime-backed (R3.1), like the Sound page: nothing is staged, nothing is dirty, and the store/Apply pipeline are never involved — a bespoke core model + `ui/` glue, not the declarative framework. The status source is `nmcli -t -f NAME,TYPE,DEVICE connection show --active`: the active-connection list answers "which network am I on?" by name across Wi-Fi/wired/VPN/WireGuard (where `nmcli general`'s one-word state and `nmcli device`'s per-interface view do not), `-t` is nmcli's documented machine-readable mode (locale-independent, script-stable), and pinning `-f` to the three rendered fields insulates against future column changes. The parser honours nmcli's terse escaping (`\:`/`\\` inside values, so an SSID with a colon survives), filters the always-active `lo` loopback record NetworkManager ≥ 1.42 lists, skips non-record lines, and maps the common type identifiers to plain labels (`802-11-wireless` → "Wi-Fi", falling back to the raw type). A non-zero exit or spawn failure degrades to a distinct `Unavailable` status ("could not read", not the misleading "no active connections") — never a panic.
  - The "Open Network Settings" button delegates deep management (requirements §3): `nm-connection-editor` preferred when installed (a native GUI needing no terminal), else `kitty -e nmtui` (nmtui needs a terminal; `nm-applet` is already autostarted, so a bare relaunch of either is not viable), and with neither available the button is omitted and logged at `info` (R4.2). Both are long-running interactive programs, so the launch is `setsid --fork <tool>…` run as a **detached** command — a new `Command::detached()` mode on the `CommandRunner` seam (a review fix): `setsid` forks the tool into its own session and exits at once (reaped, no zombie), *and* the runner spawns a detached command with its output streams discarded (`/dev/null`) instead of captured — necessary because the forked tool inherits capture-pipe write ends, so a capturing run would sit draining them until the tool exits, freezing the GUI for the tool's whole lifetime despite `setsid` returning instantly. The task-4.4 hypridle respawn had the same latent stall and now runs detached too; success of a detached launch confirms only the fork — an exec failure inside the detached session is not observable, and the rustdoc says so. `Binary::NmConnectionEditor` follows the existing injectable `$PATH`-scan pattern; its absence is logged at `debug`, not as a hidden item, since kitty is the fallback.
  - Page behaviour: rows render name + type (device) inside an "Active connections" frame, with a "Refresh status" button and a re-read on every page entry (the same persistent-stack wiring as the Sound page), and the launcher button carries a tooltip naming the tool it opens. The buttons are static — a refresh rebuilds only the status frame's contents, never the button being clicked — and the *first* `nmcli` read is deferred to first page entry (build runs no `nmcli`), so a wedged NetworkManager can never hold populate — and every category's appearance — for the 5 s command timeout against the R8.1 startup budget. The category was already `nmcli`-gated by task 5.1 (hidden + logged at `info` without NetworkManager, R4.2/R4.4 — covered by the existing `category.rs` tests).
  - Verified: `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, and `cargo test` (455 unit + the `module_boundaries`/`no_custom_css` guards) all green. New headless tests (via `MockCommandRunner`, R6.1) cover the exact pinned `nmcli` arg vector, the terse parse (escaped colon decoded, loopback filtered, device-less VPN, malformed lines skipped, unknown type raw), the failure degradations to `Unavailable`, the launcher preference per capability combination (editor+kitty → editor; editor only; kitty only → nmtui; neither → hidden), and that driving the full decision + launch records exactly the **detached** `setsid --fork nm-connection-editor` / `setsid --fork kitty -e nmtui` (the detached flag participates in `Command` equality, so the mock assertions prove the no-capture mode is used). A real-runner regression test pins the fix itself: `setsid --fork sleep 30` run detached must return well under a second — through the capturing path it blocks on the inherited pipes for the child's lifetime. The real `nmcli -t` output on the target machine was inspected read-only and matches the parser's expectations (including the loopback record); the GUI and the management tools were deliberately not launched during verification.

## 7. Test infrastructure & suites (beyond per-task unit tests)

- [x] **7.1 Fixture dotfiles tree** — Complexity: 2
  Anonymized copy of the real dotfiles layout under `tests/fixtures/` — including the post-prep files (`config/hypr/input.conf`, `config/gtk-{3,4}.0/settings.ini`, `theme/fonts`, the generated font partials, `state/active-scheme`; analysis §6), installable into a `tempfile` dir with symlinks mirroring deployment (R6.1). *Accept:* helper produces a working tree per test; used by ≥ all integration suites.
  *Done:* implemented as the fixture snapshot in `tests/fixtures/dotfiles/` + the installer `src/testing.rs`, smoke-tested by `tests/fixture_tree.rs`:
  - The snapshot is an **anonymized** copy of the post-prep layout (analysis §6): `colors/{everforest,nord,README.md}` (full 17-key schema; the README exercises enumeration skipping), `scripts/generate-colors` (an executable stub that exits non-zero — tests drive the generator through `MockCommandRunner`, so a real invocation is a test bug surfaced loudly), `state/active-scheme`, `theme/fonts`, `zsh/colors.zsh`, and `config/` with every parser target (`hypr/*` incl. `input.conf`, both `gtk-{3,4}.0/settings.ini`, `uwsm/env` with the commented `GTK_THEME`, `swaync/config.json`) plus the generated color/font partials with their `# Generated from …` headers and the non-target files deployment also links. Anonymization: no usernames/serials/locations; home paths use a `/home/user` placeholder the installer substitutes, monitor `desc:` strings are neutral but keep the exact awk-parseable record shape; long freeform files (`hyprland.conf`, swaync `style.css`) are trimmed stand-ins keeping every load-bearing shape (documented in `tests/fixtures/README.md`).
  - `FixtureDotfiles::install()` materializes a fresh, isolated tree per test into a `tempfile::TempDir`: repo at `<home>/.dotfiles`, per-file symlinks from `<home>/.config` exactly like `setup.sh` (plus `~/.zsh_colors`), placeholder substitution so the wallpaper path points at a real stub file (passing the R8.3 image validator), and permission preservation. The handle exposes `home()`/`repo_root()`/`config_root()`/`config_path()`/`repo_path()`/`wallpaper_path()`; tests inject these paths through the existing seams (`DetectionInputs`, parser/file paths) rather than mutating env vars, which are racy under the parallel harness.
  - Groundwork for 7.2 (integration crates need library access): the crate now builds as **lib + thin bin** (`src/lib.rs` declares the modules; `src/main.rs` only parses the CLI and calls `ui::app::run`), the headless layers (`core`/`parsers`/`system`) are `pub` throughout (a test-access contract, not a semver surface), and the test-support code — this installer plus `MockCommandRunner`/`MockProcessSignaller`/`Capabilities::for_tests` — is gated `#[cfg(any(test, feature = "testing"))]` with the `testing` feature enabled for test targets via a self-referential dev-dependency, so it never reaches a release build.
  - `tests/fixture_tree.rs` (10 tests) proves the tree per the accept criteria: every deployed path is a symlink resolving into the repo (and writes through it), real `Capabilities::detect` discovers the palette source behind the deployed anchor symlink (R8.5), and one smoke test per parser family reads its file at the deployed path with byte-identical round-trips and the documented values (17-key palettes, `input.kb_layout=us,se`, the three positional `listener` timeouts, the unified wallpaper path validating, swaync position/timeout with stable key order, the unified cursor values in both INIs, the commented `GTK_THEME` override, active scheme `everforest` from the generated header). The module-boundary and no-custom-CSS guards still pass unchanged; end-to-end Apply suites are task 7.2.

- [x] **7.2 End-to-end staged-edit → Apply suites** — Complexity: 3
  Per category: stage edits against the fixture tree, Apply with mock runner, assert exact resulting file bytes + exact reload command list, including rollback-on-failure and reload-failure scenarios (R5.4–R5.5). Consolidates the per-page Apply assertions from §6 into the shared fixture harness — page tasks may reference this suite rather than re-specify byte-exact tests. *Accept:* one suite per file-backed category; failure-injection cases green.
  *Done:* implemented as one integration suite per file-backed category — `tests/apply_display.rs` (3), `tests/apply_theme.rs` (8), `tests/apply_input.rs` (3), `tests/apply_notifications.rs` (2), `tests/apply_power.rs` (3) — over a shared harness added to `src/testing.rs` (Sound is runtime-only and Network read-only, so neither has a suite):
  - **The harness drives the app's real code paths, not test re-implementations.** `src/testing.rs` re-exposes the crate-private startup loaders (`testing::loaders::{input_conf,swaync_config,hypridle_conf}` wrapping `ui::startup`, now `pub(crate)`) so the store is loaded — and its conflict re-reader registered — exactly as task 5.4 does, plus `base_apply_plan` (wrapping `ui::window::interim_apply_plan`) so plans are assembled by the window's own builder before each suite folds in the page writes in the window's order. New shared assertions: `repo_snapshot`/`assert_repo_untouched_except` (tree-wide "only the planned files changed, everything else byte-identical, no file created/deleted"), `replace_once` (expected bytes = the original with exactly one span patched — the surgical-edit contract made executable), and `expect_applied`.
  - **Happy paths, byte-exact at the deployed symlinks (R5.3/R8.5):** Display rewrites only the eDP-1 `monitor=` record (mode+scale, `bitdepth,10` extras and every comment preserved — awk-parseability for `hypr-display-profile.sh`) then exactly `hyprctl reload`; Input reorders `kb_layout` + flips a touchpad toggle then `hyprctl reload`, and proves the commit → second-apply loop (no self-conflict, R5.6); Notifications decomposes `bottom-left` into `positionY`/`positionX` + timeout with JSON key order stable, then `swaync-client -rs`; Power edits `listener[1].timeout` leaving the other listeners byte-identical, covering **both** hypridle restart mechanisms (`systemctl --user is-active`→`restart`, and the failed-probe SIGTERM + detached `setsid --fork` respawn); Theme covers the palette switch (source discovered by real detection through the deployed `colors.conf` symlink; `generate-colors nord` then the hyprctl/eww/swaync/kitty-SIGUSR1 chain), the GTK theme written to both `settings.ini` duplicates, the cursor written to all four copies identically (+ the deduped gsettings/setcursor reloads), the wallpaper dual write (hyprpaper+hyprlock, preload+`,path,cover` reload, no hyprlock reload), and the 7.1-review **tilde-shaped original** (`~/Pictures/…` rewritten into the installed fixture before load, then surgically replaced by a staged absolute path). Every happy path also asserts write-through to the repo target behind the preserved symlink and the untouched-tree invariant.
  - **Failure injection (all green):** *rollback* — a two-category plan (Input write, then swaync write made to fail via a read-only repo `swaync/` dir, root-guarded) restores the already-written `input.conf` byte-exactly with no reload run (R5.4), and a combined GTK-theme + palette apply whose `generate-colors` exits 1 rolls back both `settings.ini` writes with the generator as the *only* recorded command — proving writes-before-generator ordering and that a rolled-back palette apply leaves every generated partial byte-identical (the gotcha's rationale); *reload failure* — Display/Input (failed `hyprctl reload`) and Power (failed `systemctl restart`) all complete with the new bytes standing and the failure reported (R5.5); *conflict* — an external `input.conf` edit between load and Apply aborts as `Conflicted` with no writes and no commands (R5.6), while Display/Theme (model-owned freshness, checked by the window *before* the plan is built) assert their `check_conflict()` guards trip instead.
  - Verified: `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test` (488 total: 452 lib unit + 3 bin + 19 new suite tests + the fixture/module-boundary/no-CSS guards), `cargo test --features testing`, and a plain `cargo build` all green. No production behavior changed — the only `src/` edits are visibility seams (`pub(crate)` on the three loaders, `interim_apply_plan`, and the `ui::startup`/`ui::window` modules) plus the documented harness additions to the test-only `src/testing.rs`.

- [x] **7.3 Startup-time benchmark check** — Complexity: 2
  Reproducible measurement of cold start to first frame (e.g. `GTK_DEBUG=interactive`-free timer + logged mark), tracked against the 500 ms budget (R8.1). *Accept:* number produced in CI or a documented manual procedure; current value recorded.
  *Done:* implemented as permanent in-binary instrumentation plus the documented manual procedure in `docs/benchmarks.md` (CI has no Wayland compositor, so the Accept path is the documented procedure):
  - **Instrumentation:** `main` captures a `std::time::Instant` as its very first statement (so CLI parsing, logging setup, GTK init, and window construction are all inside the window) and passes it to `ui::app::run`; on the *first* window build (never on an R8.4 re-activation) `arm_first_frame_mark` (`src/ui/app.rs`) hooks the window's first frame-cycle tick and logs `startup_ms` at `info` from that cycle's GDK frame-clock **`after-paint`** — the closest observable point to "the window is on screen" (GTK has rendered and handed the frame to the compositor; earlier signals like `map` or the tick itself fire before rendering and would flatter the number). Both hooks unregister themselves after firing, so steady-state overhead is zero; the timing plumbing that touches GTK stays in `ui/` (layering rules), with only a plain `Instant` passed in.
  - **Current value (2026-07-20, i7-8550U Arch/zen, Hyprland 0.55.4):** 5 release-build runs measured 371/251/253/260/249 ms — **median 253 ms**, worst (first `exec` of the fresh binary, the coldest standard sample) 371 ms — all inside the 500 ms budget. Warm-cache caveat (true cold-from-disk needs a root cache drop and is documented but not standard) recorded in `docs/benchmarks.md` with the full table. Notably the startup worker finished 35–50 ms *before* the first frame in every run, so each measured frame was already the fully populated window, not the loading placeholder.
  - The measurement runs were read-only (verified before launching: the only commands startup spawns are read-only probes — `hyprctl monitors all -j`, plus `swaync-client --get-dnd` when swaync is present; Apply never clicked, no daemon touched) and closed via `timeout`'s SIGTERM. Verified `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test` (452 lib + integration suites), `cargo test --features testing`, and `cargo build` all green. Deliberately deferred: no headless CI timing assertion — `ui::startup::load` probes the real host (PATH scan, procfs, live config paths), so any CI-side bound would measure the runner's machine, not the app, and be flaky padding.

## 8. Polish & release

- [x] **8.1 Structured logging audit** — Complexity: 1
  Verify R7.3 coverage: detection results, every write (path + keys, no full contents at `info`), every reload + exit status, all errors; `debug` includes parsed values and staged diffs. Also confirm no operation requires root or writes outside `~/.dotfiles`/`~/.config` (R8.2). *Accept:* checklist walkthrough against `journalctl` output for a full Apply cycle.
  *Done:* audited every `tracing` call site against the R7.3 checklist and recorded the walkthrough in `docs/logging_audit.md`, driven by the new `examples/logging_walkthrough.rs`:
  - **Coverage held for (a)–(d)** — detection results (per-item `debug`, hidden items `info`, summary `info`), every write at `info` as two layered records (writer: resolved path + byte count; apply: live path + changed keys — never contents; the only output-adjacent log, the command stderr excerpt, is truncated and `debug`-only), every reload + exit status (reload-level in `core/reload.rs` plus the runner-level `ran command` with `exit_code` for every spawned process), and all abort/failure paths at `warn`/`error`. **Three (e)-gaps fixed:** `SettingsStore::stage` now logs the original → staged values (the diff, not just the id), the parsed `(SettingId, Value)` pairs are logged at `debug` in `SettingsStore::ingest` (the choke point shared by initial load and conflict reload), and `PaletteModel::stage` logs the pending scheme switch (a palette switch writes no file, so no parser edit log would otherwise name it).
  - **The Accept walkthrough** runs the real pipeline headlessly against the task-7.1 fixture tree with the real journald subscriber (`cargo run --example logging_walkthrough`): real detection, real store loads, a happy-path Apply (real atomic write through the deployed symlink, mocked reload runner so the live compositor is never poked — justified in the doc), a commit, and a rollback Apply whose `generate-colors` stub runs as a *real* subprocess exiting 2, so the runner's exit-status line, the write-failure `error`, and the per-file restore all land genuinely in `journalctl --user -t settings4000`. The captured, trimmed journal lines are walked item-by-item in `docs/logging_audit.md`.
  - **R8.2 confirmed** (evidence in the doc): no `sudo`/`pkexec`/setuid anywhere, `systemctl` always `--user`, one write primitive (`write_atomic`, canonicalizing the live XDG path with the temp file beside the resolved target), targets built only from the XDG config home or the discovered repo root — with the laptop-display hotplug override in `/tmp` documented as the one deliberate, user-owned runtime-flag exception (per the CLAUDE.md hotplug gotcha). Verified `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test`, `cargo test --features testing`, and `cargo build` all green.

- [x] **8.2 Desktop integration** — Complexity: 1
  `.desktop` file + icon, install target (`~/.local/share/applications` or package). *Accept:* launchable from rofi; single-instance activation works from the launcher.
  *Done:* implemented as `data/org.settings4000.Settings4000.desktop`, `data/org.settings4000.Settings4000.svg`, and the per-user installer `install.sh`, with a new headless guard test in `src/ui/app.rs`:
  - **Everything is named after the fixed GApplication ID.** On Wayland, GTK sets each window's `app_id` to the GApplication ID and compositors/launchers associate the window with the desktop file of the same name, so the desktop file name, its `StartupWMClass` and `Icon` values, and the icon file name all carry `org.settings4000.Settings4000` (the `APP_ID` constant, R8.4). A new unit test (`desktop_entry_and_icon_carry_the_application_id`) fails headlessly if `APP_ID` and `data/` ever desync. The icon is an original SVG drawn for this project (three slider tracks with offset knobs on a rounded plate) — no third-party artwork.
  - **Install mechanism (`install.sh`, repeatable, no root, R8.2):** `cargo build --release --locked`, then `install` of the binary to `~/.local/bin/settings4000`, the icon to `$XDG_DATA_HOME/icons/hicolor/scalable/apps/`, and the desktop entry to `$XDG_DATA_HOME/applications/` with its `Exec=` line rewritten to the absolute binary path (the Desktop Entry spec expands no `~`/`$HOME`, and `~/.local/bin` may not be on the launcher's PATH; the rewrite is a line-by-line copy, not `sed`, so no replacement-escaping pitfalls). The installed entry is `desktop-file-validate`d, `update-desktop-database` refreshes the launcher cache, and `gtk-update-icon-cache` runs only when a per-user `index.theme` exists (the tool refuses otherwise; icon lookup works uncached). `--uninstall` removes exactly the three installed files.
  - **Accept verified live (2026-07-21, Hyprland session):** the installer produced exactly the three files above; `desktop-file-validate` passed on both the repo template and the installed entry; `gtk-launch org.settings4000.Settings4000` — the same .desktop resolution rofi's drun mode uses — started the app (one process), a second `gtk-launch` presented the existing window instead of starting a new process (process count stayed 1, same PID; journal logged the R8.4 "another Settings4000 instance is already running; activating it and exiting" line), and `hyprctl clients` showed the window's class as `org.settings4000.Settings4000`, confirming the window↔desktop-file↔icon association. The app was opened read-only and closed with SIGTERM; Apply was never clicked and rofi's config untouched. Verified `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test` (453 lib + integration suites), `cargo test --features testing`, and `cargo build` all green.

- [x] **8.3 README + install/build docs** — Complexity: 1
  Build/run instructions, dependency list, relation to the dotfiles prep tasks (`~/.dotfiles/settings_app_prep_tasks.md`). *Accept:* clean-machine build succeeds following the doc.
  *Done:* wrote `README.md` at the repo root — the one entry-point document for an outside reader:
  - Covers what the app is (one paragraph), build dependencies (MSRV 1.85 from `Cargo.toml` `rust-version`, CI floating on stable; GTK 4 ≥ 4.10 dev libraries with the Arch/Debian package names matching what `.github/workflows/ci.yml` installs), the detected-at-runtime tools presented as optional capabilities (not build deps — detection degrades gracefully, R4), build/run + the three CI gates, the note that plain `cargo test` already covers the integration suites (the `testing` feature reaches test targets via the self-referential dev-dependency, so no extra flags), the logging-walkthrough example and benchmark-procedure pointers, `install.sh`/`--uninstall` + single-instance behavior, journald logging with the R7.2 level precedence, the XDG-path/symlink-follow addressing (works with or without the dotfiles deployment, R8.5), the relation to the completed `~/.dotfiles/settings_app_prep_tasks.md` restructurings (verified against that file read-only; layout documented in analysis §6), and one-line pointers to every `docs/` file. No license section (no license file exists) and no badges.
  - Accept approximated as honestly as possible without a clean machine: a **fresh `git clone` into an isolated directory** built (`cargo build`) and tested (`cargo test`, 489 passing) from scratch following the README's commands, all three gates ran green in the repo, `cargo run --example logging_walkthrough` ran as documented, and the Debian package name was cross-checked against the CI workflow (which is itself a from-clean `libgtk-4-dev` + stable-toolchain build on every push). Not verified: an actual pristine machine/user account and the Arch package instructions (this host already has `gtk4`/`base-devel` installed).

## 9. Post-review fixes (2026-07-21 full-project review)

Findings from the post-completion review (seven scoped review passes over the whole codebase; every slice "approve with minor changes", CI gates green). Grouping and order: hygiene first (9.1–9.2 — removing the stale lint allowances may surface further dead code, so do these before anything else), behavioral fixes (9.3–9.11), test gaps (9.12–9.13), then readability refactors (9.14–9.25). The review's overall readability verdict was A-: production naming and decomposition are idiomatic; the "convoluted" impression traces to duplicated prose and duplicated glue code, which 9.14–9.22 remove.

- [x] **9.1 Remove stale `dead_code` allowances and outdated module docs** — Complexity: 1
  The per-module `#[cfg_attr(not(test), allow(dead_code))]` attributes in `src/core/mod.rs`, `src/system/mod.rs`, and `src/parsers/mod.rs`, and their "consumer does not exist yet / remove once 4.5/5.3/6.x lands" justifications, are stale by their own stated removal condition — every named consumer has landed (`ui/window.rs` drives `apply::run`/`commit_apply`; every parser is imported by `core/`). Delete the allowances, resolve anything clippy then flags (that is genuinely unused surface worth knowing about), and rewrite the module comments in present tense describing the actual consumers. *Accept:* no `cfg_attr(not(test), allow(dead_code))` remains under `src/`; `cargo clippy --all-targets -- -D warnings` green; no module doc claims a consumer "does not exist yet".
  *Done:* deleted all 16 allowances across the three `mod.rs` files and rewrote each module comment in present tense naming its verified consumers (grep-checked per module). Clippy surfaced no dead code, even from a clean build — every module is `pub` at the lib root as the R6.1 test-access contract, so `dead_code` cannot fire on that surface anyway; the one genuinely test-only production item, the palette parser's `set_value`, is kept and documented as part of the shared lossless parse/edit/emit contract (architecture §3).

- [ ] **9.2 Replace insider review-tag comments and stale "interim" naming** — Complexity: 1
  Comments referencing review threads that exist nowhere in the repo — "task 6.6 review M1" (`ui/window.rs:1063`), "the Input review-M1 contract" (`ui/window.rs:1101`), "(N3 review fix)" (`ui/sound.rs:69,77,119`), "task 6.7 review S1" (`ui/page.rs:886`), "task 6.3 review S2" (`ui/theme.rs:237`), "the S1 review fix" (`ui/category.rs:274`) — violate the CLAUDE.md outside-reader rule; replace each tag with the one-line reason itself (most comments already state it — drop the tag). Rename `interim_apply_plan` (`ui/window.rs:1414`) — a permanent, load-bearing function — to `base_apply_plan`, fix `wire_apply`'s rustdoc still claiming "validations only, no writes yet", and resolve the stale `TODO(§6)` in `ui/chrome.rs:142`. *Accept:* grep for review-tag shorthand, `interim_apply_plan`, and the stale TODO finds nothing; the affected docs describe current behavior.

- [ ] **9.3 Manual Refresh rebuilds the category models (R4.3)** — Complexity: 3
  `wire_refresh` (`ui/window.rs:1343`) re-runs detection and `store.refresh()` but never rebuilds the bespoke models (display/input/notifications/power/palette/themes/wallpaper) — they are only filled by the startup worker — so refreshed capabilities and rendered content diverge until app restart (e.g. install hyprpaper → Refresh → the wallpaper section stays hidden). Have Refresh re-run the same loader path as startup (the worker `startup::load`, or the per-model builders) against the new `Capabilities` before `populate()`. *Accept:* headless test: a capability or model source changed between load and Refresh is reflected after Refresh (model rebuilt, section appears/disappears); staged store edits survive per the existing refresh contract.

- [ ] **9.4 Defer Sound enumeration off the populate path** — Complexity: 2
  `sound::build()` (`ui/sound.rs:305`) runs `pw-dump`/`wpctl status` synchronously on the GTK main thread inside `Shell::populate` — the exact stall `ui/network.rs:29` documents avoiding (a wedged daemon holds a synchronous probe for the full 5 s command timeout, delaying every category's appearance) — and re-enumerates on every Refresh repopulate even for a never-visited page. Adopt the Network pattern: placeholder in `build()`, first enumeration on first page entry via the existing `wire_sound_page_entry` hook. *Accept:* no subprocess spawn during `populate`; enumeration happens on first entry and the existing rescan paths; headless tests updated.

- [ ] **9.5 DisplayModel: abort Apply on failed write preparation; validate staged positions** — Complexity: 2
  `DisplayModel::apply_contribution` (`core/display.rs:615`) maps a render failure to `None` — indistinguishable from "nothing dirty" — so the window can skip the write yet still `commit()` and re-baseline against bytes that were never written; Input/Notifications/Power deliberately return `Err` and abort in exactly this case. Change the signature to `Result<Option<DisplayApply>, …>` and abort like the siblings. Also validate `stage_position` (`core/display.rs:520`) — the one staging path with no validation — as `auto` or `XxY` with integer axes (no comma/`#`), and include position in `staged_validations` (R8.3: the model is the enforcement layer, not the curated drop-down). *Accept:* headless test: an injected render failure aborts the Apply with staged edits retained and no commit; an invalid position is rejected at stage time.

- [ ] **9.6 Theme models commit only rendered writes** — Complexity: 2
  `ThemesModel::commit` (`core/theme.rs:909`) promotes all four selections even when `render_settings_ini`/`render_uwsm_env` skipped a key on a `set_value` error, and `WallpaperModel::commit` (`core/theme.rs:1781`) derives `wrote_lock` from `hyprlock_write_needed()` rather than from whether `render_hyprlock` actually produced a `FileWrite` — both can re-baseline the model to a value that reached no file (model-vs-disk desync; brushes R3.4's "every copy identical"). Derive commit promotion from the rendered `FileWrite`s (e.g. `writes.iter().any(|w| w.backing == …)` / match promoted selections against `changed_keys`). *Accept:* headless tests: a failed render leaves the affected selection dirty; a skipped hyprlock write leaves the lock field un-promoted.

- [ ] **9.7 Add the built-in Adwaita variants to GTK-theme discovery (R3.3)** — Complexity: 1
  R3.3 lists installed theme dirs "plus the built-in Adwaita variants", but neither `discover_gtk_themes` (`core/theme.rs:1254`) nor the loader appends them — on a system where GTK 4's compiled-in Adwaita has no `/usr/share/themes/Adwaita/` directory, Adwaita silently disappears from the drop-down (an undocumented scope reduction). Merge a `BUILTIN_GTK_THEMES` constant into the `Selection` options (deduped against scan results, sorted), with a doc comment citing R3.3. *Accept:* unit test: a fixture tree without an Adwaita dir still offers Adwaita; a scanned Adwaita dir is not duplicated.

- [ ] **9.8 hyprlang edits target the last occurrence of a duplicated key** — Complexity: 2
  `resolve_path` (`parsers/hyprlang.rs:490`) edits the **first** occurrence of a duplicated key within a section, but hyprland applies config top-to-bottom (last assignment wins), so an edit to a shadowed first copy would silently not take effect. The env (`parsers/env.rs:419`) and INI (`parsers/ini.rs:383`) editors deliberately target the last occurrence for exactly this reason; the divergence has no stated justification. Make `resolve_path` remember the last matching entry; flip the pinning test (`edits_only_the_first_occurrence_of_a_duplicate_key_in_a_section`) to assert last-occurrence and document the rule in the module doc (architecture §3 updated alongside this task list). *Accept:* round-trip identity unaffected; the duplicate-key fixture edits only the last copy.

- [ ] **9.9 swaync config reload uses the verified flag** — Complexity: 1
  Per `swaync-client(1)`, `-rs`/`--reload-css` reloads CSS only; config reload is `-R`/`--reload-config` — so a Notifications-page apply (position/timeout in `config.json`) likely writes correctly but does not take live effect (R5.3). Verify against the target swaync, then have `SwayncReload` (`core/reload.rs:379`, mapping at `:149`) issue `-R` for a `config.json` change, keeping `-rs` in the palette chain (whose `generate-colors` rewrites `swaync/colors.css`). Architecture §6 and R5.3 have been updated to this contract alongside this task list; this closes the open question carried since the 6.7 review. *Accept:* live verification recorded here; mock-runner tests assert the exact flag per change class.

- [ ] **9.10 Surface `generate-colors` stderr in Apply failures; guard plan drift** — Complexity: 1
  On a non-zero generator exit only the exit code reaches `WriteFailureCause::GenerateColorsExit` (`core/apply.rs:457`), dropping stderr — the actionable diagnostic (e.g. the documented `theme/fonts` abort) — even though `system/command.rs:50` explicitly promises callers the full captured output for exactly this case. Carry a bounded stderr excerpt (e.g. first 512 bytes, lossy UTF-8) in the variant and render it in its `Display`. Also add the guard `apply.rs:173` documents as missing: warn (or debug-assert) when `ApplyPlan.writes` is non-empty but `validations` is empty, so the R8.3 validation gate cannot pass vacuously for a drifted caller. *Accept:* a failure-injection test asserts the excerpt appears in the rendered outcome; the drift guard is covered.

- [ ] **9.11 An unreadable tracked file must not block unrelated applies** — Complexity: 2
  Step 2's conflict check is whole-tracker scoped (`core/apply.rs:338`), justified by "the store re-baselines on focus/refresh" — but for `ConflictReason::Unreadable` that is false: `refresh()` (`core/store.rs:527`) reports the failure and never re-baselines or untracks, so once any tracked backing file is deleted/unreadable, every apply of every category returns `Conflicted` until the file reappears or the app restarts (safe, but a recovery-free dead end). Either scope the conflict check to the plan's write targets, or let refresh untrack a persistently-unreadable file after surfacing it to the user. *Accept:* headless test: with one tracked file deleted, an apply touching only other files succeeds; the unreadable file's own category still conflicts/degrades safely.

- [ ] **9.12 Icon-theme test coverage** — Complexity: 1
  The icon-theme control is the one shipped, user-reachable control with zero regression coverage: `stage_icon_theme` (`core/theme.rs:818`) is called only from production `ui/theme.rs:387`; nothing exercises the `KEY_ICON_THEME` dual `settings.ini` write (`core/theme.rs:998`) or the `reload_params.icon_theme` gsettings contribution (`:1156`). Add a unit test mirroring the GTK-theme one — both `settings.ini` copies patched identically plus the exact `gsettings set org.gnome.desktop.interface icon-theme …` command — and/or fold an icon case into `tests/apply_theme.rs`. *Accept:* a deliberate break in the icon-theme write or reload fails the suite.

- [ ] **9.13 Harden and deduplicate the lexical guard scanners** — Complexity: 1
  `contains_path_prefix` (`tests/module_boundaries.rs:310`) misses a bare globally-qualified expression such as `::gtk4::Window::new()` (legal with no `use`, since gtk4 is a direct dependency): when the preceding char is `:`, look one char further back to recognize a global-path `::` qualifier and flag it. The ~110-line `rust_sources`/`strip_comments` lexer is duplicated verbatim between `tests/module_boundaries.rs:153` and `tests/no_custom_css.rs:86` — share one copy (e.g. `tests/lexical_guard/mod.rs` pulled in via `#[path]`), and move the four verbatim copies of the `differing_line_indices`/`differing_lines` test helper (hyprlang/env/ini + `core/input.rs:461`, `core/power.rs:356`) into shared test support. *Accept:* an injected `::gtk4::` reference in `core/` fails the guard; one lexer copy and one diff-helper copy remain.

- [ ] **9.14 Split `core/theme.rs` into a directory module** — Complexity: 2
  At 3,592 lines the file holds three independent staging models — `PaletteModel` (`:129`), `ThemesModel` (`:591`), `WallpaperModel` (`:1496`) — plus shared value types and ~1.6k lines of tests; the `// ====` banner comments at `:377`/`:1366` are the file admitting it wants to be several. Convert to `core/theme/{mod,palette,themes,wallpaper}.rs`: `mod.rs` keeps the shared staging primitives (`Selection`, `PathField`, `BackingText`, `read_backing`) and re-exports so callers don't change; each submodule takes its model, its slice of the 83-line module doc (trimming the duplication while there), and its tests. Purely mechanical, no behavior change. *Accept:* diff shows moves plus doc trims only; all tests green unmodified apart from paths.

- [ ] **9.15 Deduplicate the theme staging plumbing** — Complexity: 3
  Within the 9.14 modules: merge `PathField` (`theme.rs:1401`) into `Selection`'s core — `effective`/`stage`/`is_changed`/`reset`/`commit` are byte-for-byte the same logic minus `options` (extract a shared `Staged { original, staged }`); extract the twin backing-file plumbing (`set_backing_text` ×2 at `:1166`/`:1918`, the identical `check_conflict`/`reload`/commit-re-render trio at `:856`/`:1736`) into a small shared `BackingSet` — the copy-paste that caused the 9.6 defect to be written twice; drop `render_settings_ini`'s four boolean parameters (`:973`) — each is just `self.<selection>.is_changed()`, readable in place. *Accept:* behavior identical (existing tests green); each duplicated block exists once.

- [ ] **9.16 Extract the Apply assembly out of `wire_apply`** — Complexity: 3
  `wire_apply` (`ui/window.rs:952-1304`) is a ~350-line click-handler closure built from three near-identical conflict-guard blocks, three store-write folds, and four model-contribution folds; its decisions (guard ordering, "abort the whole Apply and keep staged edits when a write can't be prepared", `store_writes` capture before the model folds) can only be exercised by clicking the button, against R6.2's intent. Extract a GTK-free `assemble_apply_plan(…) -> Result<AssembledPlan, PrepareFailure>` beside `base_apply_plan` (already exposed to `crate::testing`), collapsing the repeated blocks with small guard/fold helpers; the closure shrinks to assemble → run → dispatch outcome. *Accept:* the assembler is headlessly tested (conflict abort, prepare-failure abort retaining staged edits, plan composition per category); existing integration suites green.

- [ ] **9.17 One shared store-backed write helper; one Apply-seam dialect** — Complexity: 3
  `InputWriteError`/`SwayncWriteError`/`PowerWriteError` and `input_conf_write`/`swaync_config_write`/`hypridle_conf_write` (`core/input.rs:389`, `core/notifications.rs:281`, `core/power.rs:271`) are ~80 structurally identical lines ×3 — empty-dirty early return → read (map+log) → render (map+log) → wrap in `FileWrite` — plus three identical error enums with their own `Display` impls; a fourth page would copy it again. Extract one `core/` helper (e.g. `render_store_write(path, backing, dirty, render_fn) -> Result<Option<FileWrite>, StoreWriteError<E>>`), keep the genuinely domain-specific renderers and address maps, and rename the seam to the dialect the other models already use: `apply_contribution` (the noun-phrase `<file>_write` trio is an unforced second naming scheme for the same pipeline hook). Consider a shared `SettingId → hyprlang string` renderer for the two private `value_to_hypr_string` copies (`input.rs:181`, `power.rs:125`). *Accept:* one glue implementation; all five file-backed modules expose `apply_contribution`; tests green.

- [ ] **9.18 Shared line-parser support module** — Complexity: 2
  `strip_terminator` is byte-identical in five parsers (`env.rs:680`, `ini.rs:650`, `palette.rs:521`, `monitors.rs:797`, `hyprlang.rs:1055`); `line_terminator`, the `emit` concatenation loop, and `reject_unsafe_value` (differing only in the forbidden-char set) are near-identical. Add a private `parsers::line` (or `common`) module for the format-invariant helpers, keeping per-parser warning/error types and addressing models where they genuinely differ (the documented self-containment argument stands for `classify_line` and field addressing — do not merge those). Include the `swaync.rs:144` nit: `emit` should serialize `&self.root` directly rather than cloning the whole tree into a `Value`. *Accept:* ~200 duplicated lines gone; all round-trip tests green.

- [ ] **9.19 Shared UI widget helpers** — Complexity: 1
  `labelled_row` (×4: `ui/page.rs:844`, `ui/display.rs:406`, `ui/sound.rs:352`, `ui/theme.rs:714`), `note` (×4: `display.rs:417`, `sound.rs:363`, `theme.rs:725`, `network.rs:241`), `build_dropdown` (byte-identical in `display.rs:374`/`theme.rs:690`, trivial variant in `sound.rs:333`), and the `PAGE_MARGIN`/`SECTION_SPACING`/`ROW_SPACING` constant trio (re-declared in five files, two slightly divergent) move to one `ui/widgets.rs`. Pure deletion (~120 lines); removes the drift risk between copies. *Accept:* one copy of each helper/constant; UI builds identically.

- [ ] **9.20 Window shell structural cleanup** — Complexity: 2
  `Shell` (`ui/window.rs:167-261`) has 19 fields — nine of them `Rc<RefCell<Option<…>>>` model/page pairs — growing linearly per category, with the same "it is NOT a separate staging source…" field-doc paragraph repeated four times: group into `PageModels`/`MountedPages` sub-structs so `Shell` reads as window / store / models / pages / chrome, consolidating the repeated docs onto the sub-struct. Replace `populate`'s six-special-case if-chain (`:591-634`) with a `match` over `SidebarCategory` (the framework path as the `PowerAndIdle` arm). Collapse the three copies of `wire_*_page_entry` (`:887-940`) into one generic `wire_page_entry`. *Accept:* behavior identical; new categories touch one place per concern; tests green.

- [ ] **9.21 Move the startup loaders into `core/`** — Complexity: 2
  `ui/startup.rs` is ~1,000 deliberately GTK-free lines of config loading/parsing living under `ui/` — forcing the `pub(crate)` visibility hack `ui/mod.rs:75` explains, and hiding "what parses the config files into settings" from a reader scanning `core/` (architecture §2 puts GTK-free, unit-testable logic there). Move the loaders (`load_input_conf`, `load_swaync_config`, `load_hypridle_conf`, the `load_*` model builders) to `core/` (e.g. `core/load.rs`), leaving `ui/startup.rs` as thin worker-thread orchestration. *Accept:* module-boundary guard still green; `ui/startup.rs` shrinks to orchestration; no behavior change.

- [ ] **9.22 Deduplicate repeated rustdoc prose** — Complexity: 2
  The same rationale essays repeat across files: the "why a surgical, lossless parser (not a serializer)" section in five module headers (`palette.rs:24`, `hyprlang.rs:25`, `monitors.rs:35`, `ini.rs:28`, `env.rs:45`), the R7.3 warnings-carry-only-line-numbers paragraph likewise, and the commit-after-Applied contract at full length ×3 (`apply.rs:94-113`, `apply.rs:289-305`, `store.rs:377-401`). State each shared contract once at its owning module (`//! crate::parsers` already states the parser contract; pick one canonical home for the commit contract) and cross-reference from the others; keep only format-/module-specific rationale local. Every reviewer independently concluded this prose duplication — not the code — is the main source of the "convoluted" feel. *Accept:* each shared contract explained once with links; per-file docs keep local rationale; rustdoc still CLAUDE.md-compliant.

- [ ] **9.23 Naming vocabulary unification** — Complexity: 2
  Adopt one word per concept, write the conventions down (in `core/mod.rs` / `ui/mod.rs` docs), then fix the outliers: **dirty** — `is_dirty` everywhere (or document field-level `is_changed` vs model-level `is_dirty` as a deliberate two-tier rule; today `theme.rs:552`/`:1434` vs `:834` is unexplained); **discovery** — `scan_*` for filesystem/proc walks, `detect_*` for capability checks, retiring `discover_*`/`enumerate_*` (`theme.rs:1254`, `sound.rs:158`, `detect.rs:567`); **model entry point** — `load` (rename `network.rs:124` `read_status`; sound's `enumerate` → `load_devices`, or document the runtime-query exception); **UI repaint** — `rerender` (repaint from state) vs `rebuild` (recreate widgets) vs `refresh` (re-query the model), fixing the off-pattern uses (`window.rs:1371` vs `ui/theme.rs:171`/`ui/sound.rs:135` vs `ui/notifications.rs:75`); rename the confusable `GtkThemeOverrideSource` (`theme.rs:449`) vs `parsers::env::GtkThemeOverride` (e.g. `GtkOverrideOrigin`). *Accept:* conventions documented; grep shows one verb per concept.

- [ ] **9.24 Consolidate `DisplayModel`'s per-monitor accessors** — Complexity: 2
  Thirteen public methods shaped `fn xxx(&self, index: usize)` (`core/display.rs:356-467`) each index `self.monitors[index]` and panic on a stale index from the UI. Replace with `fn monitor(&self, index: usize) -> Option<MonitorView<'_>>` carrying the getters as methods on the view — one accessor plus a cohesive type, and an out-of-range index becomes `None` instead of a panic. While there: restructure `merge_monitor`'s two invariant-backed `expect`s (`:956`) as `if let` so the invariant holds structurally. *Accept:* UI call sites updated; a stale-index test asserts `None`, not a panic.

- [ ] **9.25 Trim oversized test names** — Complexity: 1
  ~60 test names exceed 60 characters, several welding two assertions with `_and_` (worst: 84 chars, `core/display.rs:1393`; `core/theme.rs:2215` and `parsers/palette.rs:813` are the same pattern). Split double-assertion `_and_` tests into two; move assertion detail into the body comments the tests already carry; aim for ≤ ~50-character behavior summaries. Cosmetic and lowest priority — sentence-style test names are idiomatic Rust and must stay; this only trims the outliers that make file listings look convoluted. *Accept:* no `_and_` double-assertion tests remain; names stay under ~60 chars barring genuine need.

## 10. Additional features

New functionality beyond the original v1 scope. 10.1 and 10.2 belong to the same release step: version 1.0 is defined as "the About menu is included", so 10.1 lands immediately before 10.2 (or in the same session), and the 1.0.0 bump must not ship without the menu.

- [ ] **10.1 Version 1.0 crate metadata (R8.6)** — Complexity: 1
  Raise `Cargo.toml` `version` from `0.1.0` to `1.0.0` and add the missing package metadata the About window needs: `authors` (the project author, GitHub handle `FiBe4000`) and `repository = "https://github.com/FiBe4000/Settings4000"`. `Cargo.toml` stays the single version source: clap already exposes `--version` via `#[command(version)]` (`src/main.rs:21`) and the startup log already records `env!("CARGO_PKG_VERSION")` (`src/main.rs:52`), so both follow the bump automatically; the About window (10.2) reads the same compile-time macros. *Accept:* `settings4000 --version` prints `settings4000 1.0.0`; a headless guard test (mirroring the task-8.2 desktop-entry guard) asserts `CARGO_PKG_AUTHORS` and `CARGO_PKG_REPOSITORY` are non-empty and the repository URL points at the GitHub project, so the About window can never render blank or desynced fields.

- [ ] **10.2 About menu (R2.5)** — Complexity: 2
  Add a `gtk::MenuButton` (standard `open-menu-symbolic` hamburger icon) to the task-5.3 `HeaderBar` carrying a `gio::Menu` with an "About Settings4000" item wired to an `app.about` action. The About window shows: application name and the task-8.2 icon, version, author, and a **clickable link to the GitHub page** — every displayed value sourced at compile time from the Cargo package metadata (`env!("CARGO_PKG_VERSION")` / `CARGO_PKG_AUTHORS` / `CARGO_PKG_REPOSITORY`), never a literal that could desync from 10.1. Constraints: no libadwaita and no custom CSS (R2.1 — `tests/no_custom_css.rs` stays green); `gtk::AboutDialog` is marked deprecated from GTK 4.10, so use it only if it compiles clean under `-D warnings` with the crate's enabled gtk4 features — otherwise a plain modal `gtk::Window` following the `ui/chrome.rs` conflict-dialog precedent, with a `gtk::LinkButton` for the repository link. Assemble the displayed strings in a small pure helper so the content is headlessly testable (R6.2). *Accept:* the menu button opens the menu and the About item shows name, version 1.0.0, author, and a clickable GitHub link; a headless test pins the helper's fields to the Cargo metadata; single-instance activation and the three CI gates unaffected.
